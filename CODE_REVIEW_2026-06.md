# 로직분석(logic-analysis) 전수 코드 리뷰 — 2026-06

대상: backend 전체(~11,860줄) + 인프라. 관점: **데이터 정확도(제안서 직결)** 최우선, 보안·안정성 병행.
방식: 6개 영역 병렬 심층 리뷰 후 종합.

---

## 🚨 0. 시급 — 운영 중단 임박 (날짜 한정)

- **[치명] Claude 모델 폐기 D-8** — `chat.py:30` `claude-sonnet-4-20250514`는 **2026-06-15 은퇴** 예정. 그날 이후 AI 채팅 + (제안서에 붙일) AI 피드백이 404로 전면 중단. → `claude-sonnet-4-6`(또는 정확도 우선 `claude-opus-4-8`)로 즉시 교체. 운영 `CLAUDE_MODEL` 환경변수도 확인.

---

## 🔴 1. 데이터 정확도 — 제안서에 들어가는 숫자 (보완 개발 핵심)

### 1-1. 추정값이 "실측처럼" 보임 (가장 큰 구조적 위험)
- **판매량(est_monthly_sales)** `main.py:1106-1136` — `검색량 × CTR표 × 0.035` 매직넘버. 근거/주석 없음, 카테고리 무시, `max(1,…)`로 **무조건 최소 월 1건 강제**(실제 0 표현 불가). 단일 키워드 기준이라 과소/과대 동시 위험.
- **리뷰/평점** `main.py:1077-1104` — 순위로 역산해 `4.7점·500건` 같은 깔끔한 숫자를 박음. 외부몰(스마트스토어 아님)은 **항상 추정**. 신규상품이 광고로 상위면 리뷰 0인데 "추정 500건".
- **크롤러 폴백** `naver_crawler.py` — 순위(자카드 유사도 0.7 폴백), 리뷰수(`max()` 휴리스틱), 평점(정규식 최빈값, "4.5kg" 오인) 등이 실제값과 **동일한 형태로 반환**되어 코드/UI가 추정인지 실측인지 구분 못 함. `except: pass`로 추락이 조용히 숨겨짐.
- **공통 권장:** 모든 반환 데이터에 출처/신뢰도 메타(`source`, `match_method`, `confidence`) 부착 → UI·제안서에서 추정값은 "추정" 배지/워터마크. **이게 정확도 개선 1순위.**

### 1-2. 명백한 계산 버그
- **[치명] 성별/연령 통계** `datalab.py:155-161,196-202` — 30일 요청해놓고 루프가 덮어쓰기라 **마지막 달 1포인트만** 반영. 제안서의 "타겟 성별/연령"이 통째로 왜곡. (주석이 버그를 정상인 양 기록)
- **연령 정규화 분모 오류** `datalab.py:206,211` — 이미 상대지수인 값을 강제로 100% 환산.
- **YoY 성장률 날짜 산술** `datalab.py:433,439` — `timedelta(days=30/365)` 드리프트로 "전년 동월" 매칭이 어긋나 누락/비대칭. → `relativedelta` 또는 (year,month) 산술.
- **페이지 번호** `naver_crawler.py:262,357` 등 — 순위를 `/40`으로 페이지 환산하는데 수집은 100개/페이지. "○페이지 노출" 수치 틀림. (sim순 API 순위를 웹페이지로 환산하는 것 자체가 정의상 무의미)
- **검색량 "< 10" → 10 올림** `naver_crawler.py:_safe_int` — 실제 1~9를 10으로. 검색량·판매량·골든키워드 전부 과대.
- **골든키워드 오판** `main.py:1399-1404` — `compIdx==""`(경쟁도 데이터 없음)를 "경쟁 낮음"과 동급 처리 → 경쟁도 미상 키워드가 "황금 키워드"로 둔갑.
- **"1페이지" 라벨 오류** `main.py:1573-1587` — display=80으로 가져와 통계 내면서 "1페이지 평균"이라 표기(1페이지=40).
- **타임존 혼용** `scheduler.py:492,504,537` — APScheduler는 Asia/Seoul인데 코드/DB는 서버 로컬·localtime 혼용 → UTC 컨테이너면 "당일 중복방지"가 날짜 경계서 오작동(중복/누락 기록).

### 1-3. 지표 일관성
- 같은 "종합점수"가 `advertiser/analyze`(base50+가산)와 `seo/analyze`(10지표 가중합)에서 **전혀 다른 공식**.
- `avg_comp_price` 표본이 엔드포인트마다 달라(crawler competitors vs 80개) **같은 상품 가격비율이 화면마다 다름**.
- 상품 매칭 로직이 advertiser/seo에서 미묘하게 달라 price 등 불일치 → 공통 함수로 추출 권장.

### 1-4. AI/예측 신뢰성
- 감성분석 `ai_insights.py:201-212` — 부정어 미처리("맛있지 않다"→긍정 오분류), `'상태'`가 부정어로 오등록, `'다시.*않'` 정규식을 `in`으로 검사해 **절대 매칭 안 됨**.
- 순위예측 `ai_insights.py:756` — 회귀 x축이 날짜가 아니라 "측정 횟수" → 측정 불규칙하면 "7일 후"가 틀림. 데이터 5건이면 R² 과적합으로 항상 "신뢰도 높음".
- median 짝수 처리 오류 `ai_insights.py:65`.

---

## 🔒 2. 보안 (종합 등급 B−)

- **[치명] 공개 리포트 무방비** `reports.py:996-1042` — `report_hash`만 알면 누구나 고객 분석(검색량·경쟁사·가격·전략) 영구 열람. 인증·만료·철회 수단 없음. → `is_public`/`expires_at` 컬럼 + `status='published'`만 노출 + 공유해제.
- **[높음] viewer 전 업체 교차 열람** `client_dashboard.py:47-48` — viewer면 소유권 검사 없이 전부 통과. `clients.py`의 `_check_ownership`(viewer 예외 없음)과 **정책 모순**.
- **[높음] 로그인 brute-force 방어 없음** `auth.py:488` — 시도 제한/잠금/스로틀 전무 (정작 리포트 뷰엔 rate limit 있음).
- **[높음] 미들웨어 fail-open** `main.py:52-53,202-203` — `API_KEY` 미설정 시 전 요청 통과, JWT 라우트 의존성에 전적 의존(라우트 누락=무인증 노출). → 운영서 미설정 시 부팅 실패로.
- **[높음] JWT 시크릿 파일자동생성** `auth.py:30-55` — gunicorn 6워커 race/재배포 시 토큰 불일치·강제 로그아웃. → `JWT_SECRET_KEY` .env 필수 주입.
- **[중간] 리포트 XSS** `reports.py:200-237` — 상품명/스토어명 미이스케이프(공개 리포트 저장형 XSS). keyword/client_name은 이스케이프됨.
- **[중간] passlib 1.7.4 + bcrypt 4.0.1 호환성 문제**, `anthropic` 버전 미고정, 에러 detail에 내부 예외 노출(reports.py).
- (참고) **하드코딩 시크릿/SQL 인젝션은 발견 안 됨** — 시크릿 전부 env, 쿼리 전부 `?` 바인딩. .gitignore 적절. deploy*.sh에 운영 IP·root@ 평문은 정리 권장.

---

## ⚙️ 3. 안정성 / 구조

- **[높음] 스케줄러 키워드 무한 누적** `scheduler.py:147-153,335-341` — 과거 분석된 키워드를 `DISTINCT`로 영원히 재추적 → API 호출이 날마다 증가, 의뢰 종료 키워드도 잔존. → 현재 등록분만 대상 또는 만료정책.
- **[높음] reports.py DB 연결설정 누락** `reports.py:85` — `busy_timeout`/`timeout` 없어 쓰기 경합 시 즉시 `database is locked`. 다른 모듈은 30s 설정. → 통일.
- **[높음] 09시 분석이 08시 추적 완료 가정(시간 고정)** — 키워드 많아지면 캐시 미완성 상태로 분석. → 완료 이벤트 체이닝.
- **[중간] 멀티워커 스케줄러 락**(flock /tmp) — 워커 크래시 시 그날 작업 누락 가능. → DB jobstore/전용 프로세스.
- **[중간] reports 조회수**: 파일 없는 리포트도 조회수 증가, view마다 쓰기 트랜잭션.
- **[중간] 업체 삭제 시 reports 미정리** → 고아 client_id가 공개리포트로 계속 노출(위 [치명]과 결합).
- **[낮음] 컨테이너 root 실행, 죽은 코드(`.replace(",",",")`), 매직넘버·타임아웃 산재, 동적 텍스트 HTML 미이스케이프(auto_analysis 리포트).**

---

## ✅ 권장 보완 순서 (정확도 + 시급도)

1. **(D-8) Claude 모델 교체** — 운영 중단 막기. (chat.py:30, + AI 피드백 붙이기 전 필수)
2. **데이터 출처/신뢰도 메타 도입** — 추정값에 "추정" 표기. 제안서 신뢰의 근본. (1-1)
3. **계산 버그 수정** — 성별/연령 덮어쓰기, YoY 날짜산술, 페이지번호, "<10"→10, 골든키워드 빈 compIdx, 타임존. (1-2)
4. **추정 → 실측 전환** — 판매량(스마트스토어 지표/리뷰증가추이), 외부몰 리뷰. (1-1)
5. **보안 3종** — 공개리포트 접근통제, viewer 권한, 로그인 rate limit. (2)
6. **안정성** — 스케줄러 키워드 누적, reports DB 설정. (3)

> 정확도(2~4)가 좋아지면 제안서 + (예정된)AI 피드백 품질이 함께 올라갑니다. 1은 날짜상 가장 급합니다.

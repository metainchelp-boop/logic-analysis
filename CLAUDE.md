# CLAUDE.md — 로직분석 작업 가이드 (필독)

> 새 세션은 작업 시작 전 이 파일을 먼저 읽는다.
> 이 저장소는 **로직분석(네이버 쇼핑 순위·SEO 분석)** 서비스다(`logic.metainc.co.kr`).
> 회사는 **여러 세션(섹션)이 동시에** 작업하므로 아래 섹션 경계와 동시작업 규칙을 지킨다.

## 🔔 섹션 공용 소식판 — 서로의 개발 단계를 항상 안다 (모든 섹션 공통 · 대표 지시 2026-09-24)

> 대표: 「섹션이 다르다고 해서 서로의 개발 단계를 몰라? 서로가 알게 해. 배포 작업 하는 과정 중에 기록 안 돼? 최신을 항상 인지하고 알고 있어야 할 거 아냐.」
> 섹션(① 전산 · ② 로직분석+맞춤제안서 · ③ 광고센터 · ④ 체험단)과 코덱스는 **서로 다른 대화**라 기억을 나누지 못한다. 그래서 **한 곳에 적고, 그 한 곳을 읽는다.** 이 문단은 다섯 저장소에 같은 글로 들어 있다.

**어디** — 저장소 `metainchelp-boop/metainc-web-frontend` 의 **`section-feed` 브랜치**(비공개 · master 가 아니라 배포되지 않는다). 다른 저장소의 세션은 `add_repo`(push)로 붙여서 읽고 쓴다.
- `SECTION_FEED.md` — **자동**: 다섯 저장소의 최근 14일 머지·배포 결과·열린 PR. 하루 네 번 · FE 배포 직후 · 최신이 당장 필요하면 FE 저장소 Actions 「섹션 소식 새로고침」을 손으로 실행한다.
- `SECTION_BOARD.md` — **사람(세션)이 쓰는 곳**: 📮 섹션 간 요청함 · 🧭 섹션별 지금(진행 중 · 배포 대기 · 다른 섹션 영향).

**읽는 때(항상)** — ① 작업 시작 ② 머지 직전 ③ 배포 직전.
- 📮 요청함에 **내 섹션 앞으로 열린 요청**이 있으면 그것부터 본다 → 착수하면 `[진행]`, 끝나면 `[닫힘 · PR 번호]` 로 고친다.
- 내가 바꾸려는 계약(API·호스트·표·같이 쓰는 파일)을 다른 섹션이 최근에 바꿨는지 `SECTION_FEED.md` 로 확인한다.

**쓰는 때(항상)** — 배포(머지)한 **그 차례에**:
- `SECTION_BOARD.md` 🧭 내 섹션 줄을 고친다(무엇을 배포했나 · 다른 섹션에 영향이 있나).
- 다른 섹션이 알아야 하거나 해 줘야 할 일은 📮 요청함에 한 줄 — `[열림] 보낸→받는 · 날짜 · 무엇 — 왜 · 근거(저장소·PR·문서)`.
- ⚠️ 비밀값·개인정보·업체 목록은 적지 않는다(근거는 저장소·PR·문서 이름으로 가리킨다). 동시에 고쳐 충돌 나면 다시 받아 **양쪽 보존**.
- 「다른 섹션에 알릴까요?」라고 대표에게 묻지 않는다 — **요청함에 적는 것이 곧 알리는 것**이다.


## 섹션 분리 구조 (4섹션) — 운영자 지시 2026-07-11

> 전사를 4개 개발 섹션으로 **명확히 분리**해 개발·배포한다. 각 섹션은 자기 저장소만 손대고,
> 경계를 넘는 의존은 **코드 직접 수정이 아니라 '통지/계약'** 으로 다룬다.

| # | 섹션 | 소유 범위 | 우리(이 저장소) 관계 |
|---|---|---|---|
| ① | **메타 전산**(=메인 전산) | 전산/ERP + **BE 서버·무중단 배포·호스트/도메인**(`api.metainc.co.kr`) | 우리는 **소비자**(SSO·제안서 저장이 전산 호스트 의존) |
| ② | **로직분석 + 맞춤제안서** | **이 저장소** + FE `metainc-web-frontend/public/proposal/` | ★ **이 섹션 = 우리** |
| ③ | **광고센터 + 광고 생성툴** | `dashboard.metainc.co.kr` + 광고 스튜디오 | 다른 섹션 |
| ④ | **단독 메타 체험단** | 체험단(독립 섹션으로 분리) | 다른 섹션(우리 범위 밖) |

> ⚠️ **별도 '인프라 섹션'은 없다**(정정 #302, 2026-07-11). BE 서버·무중단 배포 파이프라인·호스트/도메인은 **① 메타 전산 소유**다. (과거 인계서의 "VPS 이전 섹션"은 별도 섹션이 아니라 ②의 당시 작업을 가리킴.)

> ### 🔴 ③ 광고센터와 **같은 물리 자원**을 쓴다 — 통지 대상 (2026-08-31 등재)
>
> 종전 통지 계약에는 **① 메타 전산만** 있었다. 8/30 전수조사에서 ②와 ③이
> **같은 서버 · 같은 사무실 IP · 같은 self-hosted 러너 · 같은 VPS 시크릿**을 공유한다는 것이
> ③ 저장소 문서로 확인됐다(③ 문서 원문: 「같은 서버의 **로직분석(②)**·스튜디오와 동거」).
>
> **이미 사고가 한 번 났다** — 스튜디오 세션 쿠키가 `/logic/` 로도 전송돼
> 「스튜디오 로그인 → **로직분석 로그인 화면으로 강제 이동**」(③ CHANGELOG v1.0.1).
> ③이 `/etc/nginx/sites-enabled/logic-analysis` 를 직접 편집한 기록도 있다.
>
> **우리가 건드릴 때 ③에 먼저 알린다**: `logic.metainc.co.kr` nginx 설정 · 포트(우리 5050) ·
> self-hosted 러너 구성 · 서버 디스크 정리.
> **③에 부탁한 것**: 스튜디오 5060 · autobid 5070 · nginx · 러너 · `docker system prune` 시 우리에게 통지.
>
> ⚠️ **`docker system prune -af` 주의** — ③ 문서가 공유 서버 대상으로 이걸 권한다.
>    그대로 돌면 **우리 롤백 이미지·빌드 캐시까지 지워진다**(우리는 blue-green 이라 롤백 이미지가 필요하다).
>
> 🟡 **자동입찰 — 대표 회신 수신(2026-08-31): 「후속 개발을 못 하고 있다. 지금은 개발 대기 상태」**
>    ③의 자동입찰 에이전트가 「**사무실 회선에서 10분마다**」 네이버 쇼핑을 크롤하는 코드가 있다
>    (`autobid/agent/autobid_agent.py`, 418/405 차단 대응·브라우저 위장 포함).
>    **우리 수집 확장이 도는 그 맥미니·그 IP 다.**
>    ⚠️ 우리는 8/28 캡차·IP 차단 원인을 「핫스팟 IP 평판」으로만 적어 뒀는데,
>       **같은 IP 를 두 섹션이 나눠 쓰는 것이라면 원인 축이 하나 더 있다.**
>       우리만 시간당 상한을 60→40 으로 조인 셈이 된다.
>
>    ⚠️⚠️ **「개발 대기」와 「가동 중지」는 다른 축이다 — 섞지 말 것.**
>       개발이 멈춰도 이미 띄워 둔 프로세스는 계속 돈다.
>
>    ### 🔴 실측 확정 (2026-08-31) — **autobid 는 4주째 가동 중이다**
>    공유 서버 `docker ps` 실측. 컨테이너 6개 중 **3개가 autobid** 다:
>    ```
>    NAMES              IMAGE                        STATUS            PORTS
>    autobid_engine     autobid-autobid-engine       Up 4 weeks
>    autobid_watchdog   autobid-autobid-watchdog     Up 4 weeks
>    autobid_web        autobid-autobid-web          Up 4 weeks        0.0.0.0:5070->5070/tcp
>    logic-analysis     logic-analysis:latest        Up About an hour  127.0.0.1:5050->5050/tcp
>    content-studio     content-studio:latest        Up 7 weeks        127.0.0.1:5060->5060/tcp
>    ad-api             ad-dashboard-deploy-ad-api   Up 51 minutes     127.0.0.1:5051->5051/tcp
>    ```
>    ⇒ **「개발 대기」는 맞지만 「안 돈다」는 아니다.** 4주 = 8/28 캡차 사고 기간을 통째로 덮는다.
>    ⇒ `autobid_engine` 은 서버에서 `msearch.shopping.naver.com` 을 직접 부른다(③ `ranker.py`).
>
>    ### 🔴🔴 확정 (2026-08-31 14:04 KST) — **에이전트도 지금 돌고 있고, 우리와 같은 회선이다**
>    에이전트는 `logic.metainc.co.kr/autobid` 로 보고하므로 **우리 nginx 를 지난다.**
>    그 접속 기록을 대조했다(⚠️ 저장소 공개라 IP 원문은 안 찍고 일치 여부·건수만):
>    ```
>    /autobid 요청 480건 · 보낸 곳 2개
>    /api/collector 요청 2714건 · 보낸 곳 4개
>    ★ 같은 곳에서 둘 다 보낸 경우: 1개
>      → 그 곳이 보낸 양: 자동입찰 458건 · 우리 수집 2611건
>    [현재 로그 기준] 마지막 /autobid 14:03:37 · 마지막 /api/collector 14:04:27
>                     오늘 건수 — 자동입찰 192건 · 우리 수집 945건
>    ```
>    ⇒ **같은 기계·같은 회선에서 둘 다 나간다.** 조회 시각 기준 **50초 전에도 자동입찰이 보고**했다.
>    ⇒ 「개발 대기」는 맞지만 **가동은 멈춘 적이 없다**(대표 회신 = 개발 상태였다).
>
>    ### ⚠️ 그래서 우리 8/28 캡차 진단을 고쳐 적는다
>    종전 기록: 「원인은 데이터 용량이 아니라 **IP 평판**이다 … 핫스팟은 여러 사용자가 돌려쓴다」.
>    **그 문장은 절반만 맞다.** 같은 회선에서 자동입찰이 하루 수백 건을 함께 내보내고 있었다.
>    우리는 그 몫을 모른 채 **우리 상한만 60→40 으로 조였다.**
>    ⇒ 수집 속도를 다시 정할 때 **③ 몫을 포함한 회선 총량**으로 계산해야 한다.
>    ⚠️ 다만 「자동입찰이 캡차의 원인이다」로 단정하지는 않는다 — **원인 축이 하나 더 있음이
>       확인된 것**이고, 어느 쪽이 얼마나 기여했는지는 아직 안 쟀다.
>
>    ### ✅ 수집 속도 — 대표 확정 (2026-08-31): **B안 = 지금 상한(40) 유지**
>    선택지는 둘이었다 — **A** 자동입찰 몫을 며칠 실측해 상한 재계산(정확하나 며칠 걸림) /
>    **B** 지금 40 유지하고 캡차가 재발하면 그때 대응(안전하나 수집량이 눌린 채로 간다).
>    **대표 확정 = B.**
>    ⇒ **코드 변경 없음.** `HOURLY_CAP = 40` 그대로 두고, 캡차 후 자동 감속도 그대로 둔다.
>    ⚠️ **다만 「눌린 채로 간다」는 것을 알고 가는 것이다** — 유니버스 814개인데 1대가
>       하루 759~1088개를 소화하니 지금은 여유가 있다. 수집량이 700 밑으로 떨어지면
>       그때가 A안(회선 총량 실측)을 꺼낼 시점이다.
>    ⚠️ **캡차가 재발하면 제일 먼저 볼 것** — 우리 상한이 아니라 **자동입찰 몫**이다.
>       이번에 그 축을 몰라서 우리 상한만 조였다. 같은 실수를 반복하지 말 것.
>
>    ⚠️ **교훈 — 사람의 답과 필요한 답이 다른 축일 수 있다.**
>       「개발 대기」를 「안 돈다」로 읽었으면 원인을 영영 못 찾았다. 축이 다르면 직접 잰다.
>    ⚠️ **교훈 — 로그를 이어 붙일 때 순서가 곧 시간축이다.**
>       첫 판에서 `[현재, 회전분]` 순으로 붙이고 마지막 줄을 읽어 **회전 경계(00:00)를
>       「마지막 요청 시각」으로 오독**할 뻔했다. 시각 판정은 현재 로그만 본다.
>    ⚠️ **`autobid_web` 만 `0.0.0.0:5070` 로 열려 있다** — 나머지 셋(우리 5050·스튜디오 5060·
>       광고센터 5051)은 전부 `127.0.0.1` 바인딩이다. ③ 판단 사항이나 통지문에 적어 둘 것.
>
>    ⚠️ **교훈 — 「대표가 개발 안 한다고 했으니 안 돈다」로 넘어갈 뻔했다.**
>       사람의 답은 「개발 상태」였고 우리가 필요한 건 「가동 상태」였다. 축이 다르면 다시 잰다.
>    ⚠️ **진단 함정** — `hang_probe` 의 `probe()` 는 출력을 `/dev/null` 로 버리고 소요 시간만 잰다.
>       `docker ps` 를 probe 로 돌려도 목록이 안 남는다. 목록은 따로 찍어야 한다(그래서 한 줄 추가했다).
>    통지문: ③ 저장소 `docs/로직분석-통지-2026-08-31.md` (PR metainc-ad-dashboard#1)
>
>    ### 🟢🟢 정정 — **회선이 갈렸다** (실측 2026-09-18 17:16 KST · 진단 debug-place #122)
>    대표 지적(「자동입찰은 회선을 끊었는데 왜 자꾸 묶여 있다고 하느냐」)으로 **다시 쟀다.**
>    ```
>    /autobid 요청 1건 · 보낸 곳 1개        (8/31 = 480건)
>    /api/collector 요청 1,705건 · 보낸 곳 3개
>    ★ 같은 곳에서 둘 다 보낸 경우: 0개     (8/31 = 1개)
>    마지막 /autobid 15:33:42 · 마지막 /api/collector 17:16:28
>    현재 로그 — 자동입찰 1건 · 우리 수집 559건   (8/31 = 192건 · 945건)
>    ```
>    ⇒ **겹치는 곳 0개.** 자동입찰과 우리 수집은 **다른 회선**에서 온다. 보고량도 480 → 1건.
>    ⇒ **위 「같은 회선을 나눠 쓴다」는 이제 사실이 아니다**(원문은 경위로 그대로 둔다).
>    ⇒ **우리 회선 총량 계산에서 자동입찰 몫을 뺀다** — 8/31 에 「몫을 모르니 우리 상한만 조였다」고
>       적어 둔 제약이 사라졌다. 상한을 올릴 근거가 하나 늘었다.
>
>    ⭐ **교훈 — 내가 이 저장소가 스스로 적어 둔 함정에 그대로 빠졌다.**
>       8/31 기록을 **확인 없이 인용해** 대표께 「지금도 같이 쓴다」고 말했다. 그건 그날의 사실이지
>       오늘의 사실이 아니었다. **사람이 「바꿨다」고 하면 기록이 아니라 서버를 먼저 본다.**
>       ⇒ 이 단락을 인용하기 전에 **반드시 `debug-place.yml` `autobid_traffic=true` 를 먼저 돌릴 것.**
>
> ✅ **데이터 계약은 문제 없다**(8/30 전수 확인) — ③ 신규 설계(A안)는 우리 API 미사용 ·
>    ③ 수집 스케줄러 06:00 KST 로 우리 04:00 과 분리 유지 · ③ 저장소에 우리 필드 참조 0건.

**경계 원칙**
- **우리 코드만 수정한다.** 전산·광고센터·체험단 저장소의 코드·거버넌스는 건드리지 않는다.
- 전산 호스트(도메인·서버) 변경 등 **경계 너머 의존이 깨질 위험**은 직접 고치지 말고, 호스트 소유 섹션(**① 메타 전산** — BE 서버·호스트·배포 소유)에 **사전 통지·계약**으로 처리한다.
  - 우리가 전산 호스트에 의존하는 지점: `backend/auth.py`(`ERP_BASE_URL` — SSO 검증), FE `public/proposal/index.html`(`API_BASE` — 제안서 저장/조회), `backend/scheduler.py`(`/api/ad-sync/contracts` — 계약단계 동기화 **04:00**(구 07:30, 2026-07-27 변경), `ERP_AD_SYNC_API_KEY`).
  - **✅ ad-sync 소비 합의(2026-07-20).** ① 소유 `GET /api/ad-sync/contracts`를 ②도 소비(계약 만료·환불중·홀딩중 업체 자동 추적 자동 중지 / 진행·전략 관리·사후 관리 복귀 시 재개). 하루 1회 **04:00**(구 07:30 — 2026-07-27 업무시간 502 사고로 전 배치를 새벽 이동, ③의 06:00과 분리 조건은 그대로 유지)·읽기 전용·단계당 1콜. ⚠️ **호출 시각 변경은 ①에 통지 대상**(합의문에 07:30으로 기재돼 있었음). **단계명 문자열이 곧 계약**(ERP_CONTRACT_SYNC_API.md) — ① 단계명 변경 시 ②에도 사전 통지 대상. 키는 서버 `.env` `ERP_AD_SYNC_API_KEY`(미설정 시 무동작). 직원 수동 토글(`auto_analysis_manual`)이 항상 우선.
  - **✅ portal-summary 검색량 연동 활성(2026-08-04).** ①이 형식 필드 대조 후 **스위치 ON·무중단 배포 완료**(`portal.seo-auto-enabled=true`, env `PORTAL_SEO_AUTO_ENABLED=false`로 재정의 가능). **① 파서 = `ClientPortalService.parseLogicSummary()` → `VolumeRow(keyword·volume·rank)`** — `seo.keywordVolume[]` 3필드명·구조는 **계약 고정, 변경 시 사전 통지**. ① 소비 방식: 매일 **04:40 KST** 배치가 P10 계약 품목 보유 업체 대상 portal-summary 원문을 `mp_client_logic_cache`에 캐시(즉시 반영은 ① 「⟳ 지금 갱신」). ✅ **시각 정합 완료(2026-08-04, A안 채택·대표 확정)**: ① 캐시 배치 **04:40 → 08:40 이동**(② 08:00 순위 기록 후 소비 = 당일 순위 노출, ① 커넥션 분리 안전장치 동반). ⚠️ **상호 계약**: ②가 08:00 순위 기록 시각을 08:35 이후로 늦추려면 ①에 사전 통지(①의 08:40 캐시가 전일값으로 회귀함). 단기 유의: 8/4 당일은 구스케줄 배치 커버리지 76%라 일부 키워드 rank 가 「-」(8/1~3 결측 잔상) — 8/5 전량 커버부터 완전 정상.
  - **🔴 대표 확정(2026-09-23) — 수집기는 「기록」 목적 · 전일 데이터 기준. 위 「08:00 순위 기록 · ① 08:40 캐시」 약속보다 이것이 우선한다.** 대표 원문: 「순위 수집기는 전일 데이터를 보여주는게 맞지 않아? 당일거를 보려면 당일 추적을 별도로 해야 하는거고, 우리는 기록이 우선인거라 당일 봐야 할 필요성은 없어. 수집기 자체의 용도가 기록 목적인데 안전하고 정확한게 우선이야. 전산과 맺은 약속이여도 여기서 처리하고 알 수 있게 메모리 해.」 ⇒ 수집 시간대를 **24시간에 나누고 8~22시에 두 배 배정**(`collect_slot.py` · PR #265). 업체 키워드도 하루 종일 나뉘므로 **①이 08:40 에 가져가는 `portal-summary` 순위는 대부분 전일 측정값**이다. 응답 형식(`seo.keywordVolume[]` 3필드)은 그대로라 ① 파서는 안 깨진다 — 바뀌는 것은 값의 신선도(당일 → 전일)뿐. ⚠️ **①에 통지하지 않은 것은 대표 지시다**(「여기서 처리하고 알 수 있게 메모리」). ①이 「오늘 순위가 안 나온다」고 물어 오면 이 줄이 답이다. ⚠️ 당일 순위가 필요한 기능이 생기면 수집기 시간표를 당기지 말고 **별도의 당일 추적**으로 만든다(대표 원문).
  - **✅ portal-summary 검색량 제공 합의(2026-07-28).** ①이 소비하는 **`GET /api/cd/portal-summary`** 를 계약 목록에 등재(종전 누락). ① 요청으로 응답에 **`seo.keywordVolume[]`**(`keyword`·`volume`·`rank`) 추가 — 값 출처는 우리가 이미 가진 데이터(검색량=`client_analyses`→`summaryCards.totalVolume` / 순위=`client_rank_history` 키워드별 최신 `rank_position`)라 **신규 크롤링·API 호출 없음**. 제약 3가지를 ①에 고지: **최대 8개 키워드**(`LIMIT 8`) · **자동 분석 이력이 있는 업체만** · **300위 밖은 `"-"`**(04:30 추적 범위). 검색량 있는 키워드가 하나도 없으면 **`seo` 키 자체를 생략**(① 담당자 입력값으로 폴백). ⚠️ **기존 4필드(`found`·`clientId`·`insight`·`dailyReports`·`totalDays`) 의미·형식 변경은 ①에 통지 대상.**
  - **✅ clients-lookup 소비 등재(2026-09-14) — 종전 누락.** ①이 **`GET /api/cd/clients-lookup?q=`** 를 상시 소비한다(계약 목록에 없어 오늘에야 발견). 소비처 3곳 — ⑴ **공유링크 자동 연결**(`ShareLinkAdminService.tryAutoConnectLogic`: `data[]` 중 **이름 완전일치**가 **정확히 1건**이면 자동 연결, 2건 이상이면 「검토 후보」) ⑵ 관리 화면 업체 피커 프록시 ⑶ ① 진단 워크플로 표본 `client_id` 추출. **계약 = `data[]` 배열 + 그 안의 `id`(숫자>0)·`name`(원문 그대로 — 완전일치 비교 대상).** ⚠️ **`data[]` 에 비활성 업체를 섞으면 안 된다** — 동명 2건이 되어 ①의 자동 연결이 조용히 「검토 후보」로 바뀐다. 그래서 내린 업체는 **`blocked[]` 최상위 키**로만 내보낸다. ⚠️ 2026-09-14 변경(정규화 매칭·랭크 정렬·`role`·`total`·`truncated`·`blocked` 가산)은 **①의 자동 연결 결과를 바꾸지 않는다**(재현 A/B 확인) — 오히려 완전일치가 맨 앞으로 와서 **30개에서 잘려 연결이 실패하던 경우가 사라진다.**
  - **✅ 호출 다이어트 통지 합의(2026-07-20).** ②의 검색 API 다이어트(D1~D5, PR #91·#92)를 ①이 검토 — `LogicAnalysisClient` 소비 4경로 스키마 영향 없음·이의 없음 확인(① 작업 브랜치 커밋 15ecff9). 소진 재발 시 ② 90% 경보를 ①에 공유하기로.
  - 2026-07 전산 BE 호스트 이전(`metainc01.cafe24.com` → `api.metainc.co.kr`)이 통지 없이 진행돼 조용한 중단 위험이 있었음 → SSO는 신 호스트로 갱신, 제안서 폴백도 신 호스트로 교체(FE PR #301, 2026-07-11 배포 완료).
  - **✅ 통지 계약 성립(2026-07-11).** ① 메타 전산이 회신으로 **호스트/도메인/서버 변경 시 ③ 광고센터 + ② 로직분석에 사전 통지**를 확약하고, 전산 CLAUDE.md에 반영(**`my-profile` 소비자에 ② 등재**). 옛 호스트는 **전환 유예(리다이렉트/병행) 후** 내림. 현재 예정된 호스트/도메인 변경 계획 없음. → 다음 호스트 변경은 사전 통지로 우리가 무중단 대응.

## 배포 규칙

- 배포는 `main` 푸시 → GitHub Actions가 VPS로 SSH/SCP + `docker compose` 재기동(회귀 게이트 → 빌드 후 스왑 → 헬스체크 → 실패 시 자동 롤백). **정적 FE와 달리 실서비스**이므로, 배포는 운영자 지시("배포하자") 시에만.
- 업무시간 중 임의 `main` 직푸시 금지 — 미승인 작업은 feature 브랜치에 stack(미머지).
- 커밋 메시지 · 코드 · PR 본문에 **AI/모델 식별자 금지**. 한글은 UTF-8.
  - ⚠️ **이 규칙은 저절로 지켜지지 않는다 — 두 곳이 자동으로 붙인다**(2026-08-27 확인·수정).
    ① **커밋 작성자** — 이 작업 환경의 git 기본값이 `Claude <noreply@anthropic.com>` 이라,
       스쿼시 머지 때 main 커밋에 `Co-authored-by:` 로 박힌다. **착수할 때 저장소 설정을 먼저 바꿀 것**:
       `git config user.name "metainchelp-boop" && git config user.email "metainchelp@gmail.com"`
    ② **PR 본문** — 생성 도구가 본문 끝에 서명 한 줄을 붙인다. **PR 을 연 뒤 본문을 다시 확인해 지울 것.**
  - ⚠️ 이미 머지된 이력은 되돌리지 않는다(공유 히스토리 재작성이 더 위험하다). **머지 전에만 고친다.**

## 🔴 이름으로 판정하지 않는다 — 내용을 열어 보고 답한다 (대표 지시 2026-09-18)

> 대표 지시 원문: 「앞으로 내용까지 전부 확인해서 피드백 할 수 있도록 해.
> 제대로 확인도 안하고 하면 같은 일 몇번씩 반복하게 되잖아.」

**하루에 같은 실수를 두 번 했다. 둘 다 「이름을 보고 내용을 안 열었다」이다.**

1. **`/api/collector/blocked` 를 「차단당했다」로 읽었다.** 열어 보니 내용은
   `STALE_PAGE`(2페이지 클릭 뒤 내용 불변) 14건 + `TAP_PROBE`(화면이 받은 응답 요약) 14건 —
   **전부 진단 보고이고 진짜 차단은 0건**이었다. 대표께 「노트북이 차단당하고 있다」고
   보고했다가 되물으심을 받고서야 열어 봤다. 경로 이름은 그 경로가 무엇을 담는지 말해 주지 않는다.
2. **`/api/collector/health` 를 부르는 곳을 「수집 확장」으로 읽었다.** 코드를 보니
   그 경로는 **`Depends(get_current_user)` = 로그인 직원 전용**이고, **확장은 이 경로를
   아예 부르지 않는다**(`background.js` 검색 0건). 실제 정체는 **로직분석 화면을 열어 둔
   직원 브라우저**(`AppShellBar.jsx` 가 10분마다 폴링). 「어딘가에 확장이 하나 더 살아 있다」고
   보고했고 **틀렸다** — 대표가 「끊어버리면 되냐」고 물으셨는데 그대로 끊었으면
   **직원 화면을 끊을 뻔했다.**

**그래서 지킬 것**
- **경로·표·칸·플래그 이름으로 의미를 단정하지 않는다.** 그 핸들러·그 스키마를 연다.
  최소한 ⑴ 누가 부르는가(grep) ⑵ 인증이 붙어 있는가 ⑶ 실제로 들어 있는 값이 무엇인가.
- **수를 보고하기 전에 그 수의 내용을 한 번은 펼쳐 본다**(사유별·표본 몇 건).
  「14건」은 그 14건이 무엇인지 모르면 보고할 수 없는 값이다.
- **조회가 실패했는데 「0건」으로 찍지 않는다.** 실패와 0은 다른 값이다
  (이번에 `collector_blocks` 칸 이름을 `created_at` 으로 틀리게 짚고 「0건」을 찍을 뻔했다 —
  진단 출력을 「못 쟀다」로 갈라 적도록 고쳤다).
- **스키마를 추측하지 않는다.** 이번 차수에만 칸 이름을 **네 번** 틀렸다
  (`tracked_products.keywords` → 실제 `tracked_keywords` 표 · `p.client_id` → 실제 브리지 표
  `rank_link` · `clients.company_name` → 실제 `clients.name` · `collector_blocks.created_at`
  → 실제 `at`). **진단도 가짜 DB 로 먼저 돌려 보고 서버에 올린다.**

## 동시작업 충돌 방지 (항상)

1. **편집 전 동기화** — 공유 파일 수정 전 `git fetch origin` 후 최신 `main` 확인, 필요 시 rebase.
2. **다른 섹션/작업 확인** — 시작·머지·배포 각 시점에 열린 브랜치/PR 확인(`mcp__github__list_branches`, `list_pull_requests`). 같은 파일을 만지는 동시 작업이 있으면 영역 분리·순서 조정으로 충돌 회피.
3. **작업 브랜치 stack** — 각 작업은 지정 feature 브랜치에서 진행.
4. **머지/배포 직전 재확인** — 머지 직전 다시 fetch + 충돌 점검 + 회귀 테스트/빌드 검증.
5. **연속성 메모리(append-only)** — 진행 상태를 문서에 누적 기록해 다음 세션이 이어받게 한다. 기존 항목 삭제·요약 금지.

## 산출물 전달 형식 (항상 — HTML, 크롬에서 바로 열람)

- 문서·리포트·체크리스트·목업·요약 등 산출물은 **자체완결 HTML 파일**(인라인 CSS, 외부 의존성·네트워크 없이 단일 파일)로 만들어 `SendUserFile`로 전달한다. 전달본 파일명·`<title>`은 **한글**로.
- 전산 톤: 표준 파랑 `#3b82f6`, 슬레이트 회색, 한글 UTF-8.

## 현재 진행 중 작업 (append-only)

- **[🔧 구현·검증 완료 2026-09-24 · 대표 「배포하자」 대기] 📖 관리팀 순위 읽기 도우미 — 관리팀이 직접 넘긴 네이버쇼핑 화면으로 2페이지 이후 순위를 채운다(네이버 요청 0) [로직분석 · 서버+화면+새 확장 `extension-human` · 새 표 1]** — 대표 확정 4건(목록 보여 주기 · 목표 없음 · PC 번호만 · 로그인 화면도 읽고 첫 주 대조). `human_view.py`·`human_view_routes.py`(토큰은 수집기 토큰에서 따로 뽑음 · 추적 키워드 아니면 저장 0 · 1위부터 끊김 없는 순위만 · 부분=찾은 것만 · 300위/결과 끝=전 대상 + 수집분 · 대상 다 찾으면 `collector_found_done`) · 확장 권한 `storage` 하나 · 순위 규칙 파일 수집기와 바이트 동일(게이트) · 운영 칸 「📖」. 시험 65+24 · 게이트 60+95 · 앱 리허설 15 · 사보타주 13. ⚠️ 운영 중인 수집기의 `chrome.debugger` 「진짜 입력으로 클릭」(기본 켬 · #243)은 **대표 확정 「끄지 마」(9/24) — 켠 채로 유지 · 다시 묻지 말 것**. 상세 `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-24 23:07 KST · Deploy #473 · 첫 효과 실측 완료] 완료 규칙 — 추적 대상을 300위 안에서 다 찾았으면 부분 수집이어도 그날 완료 [로직분석 · 서버 · 새 표 `collector_found_done`] — PR #283 squash `5de267f`** — 9/25 09:12 실측(진단 #411 · 로그 삭제) = 부분 수집 216회·135개 중 대상 전부 찾은 19개 → **완료 표시 19개**(빠짐 0) · 다 찾은 뒤 또 잰 부분 수집 **0회**(배포 전 15회) · 오늘 완료 누계 44 · 두 기계 정상. ⚠️ 1페이지에 대상이 없는 약 300개는 이 규칙으로 안 준다(도우미 #284 몫). 상세 `docs/WORKLOG.md` 맨 위.
- **[🔁 되돌림 완료 2026-09-24 16:17 · Deploy #472 · 수집기 v1.27.1 대기] 서버 자동 배정(v2) 2번 한 대 시험 — 수집기가 「기다려」(WAIT_BUDGET)를 「이번 시간 끝」으로 받아 시간당 1~3개만 모음 [로직분석 · 서버 · PR #278·#279]** — #278(14:21) 스위치를 `collector_settings.py` 로 옮기고 2번만 켬 + 배정에도 밤 재시도 상한 → 15:01 2번 등록·배정·완료 정상 · 15:07 이후 「대기」로 멈춤(`runCoordinated` 가 `WAIT_BUDGET` 에 루프를 끝내고 `finishedHour` 를 찍음 · 키워드 사이 2~3초만 쉬어 거의 매번 걸림) → 대표 「1번 되돌리기 배포하자」 → #279 2번도 끔 · 설정 번호 3 · 2번은 17시부터 옛 방식. ⚠️ 서버 간격만 줄이는 우회는 몰아 부르기라 안 함. 다음 = v1.27.1(기다렸다 이어 받기 · 고른 간격) → 2번만 교체·재시험. 상세 `docs/WORKLOG.md` 맨 위. ⇒ **대표 확정(9/24 16:3x) 「교체하지 말고 지금 유지」** — v1.27.1 보류 · 두 대 모두 옛 방식(번호 분할)으로 운용. ⚠️ 두 노트북 팝업의 「🧭 서버 자동 배정」은 **켜진 채**이고 서버 `WORKER_OVERRIDES` 의 `swCoordinated: False` 가 막고 있다 — **이 덮어쓰기를 지우면 두 대가 곧바로 결함 있는 배정 방식으로 넘어간다**(지우려면 v1.27.1 먼저).
- **[🔧 구현·검증 완료 2026-09-24 · 대표 확정 「하루 2번만」 · 머지 대기] 🌙 밤 재시도 상한 — 밤(23시·0~7시)엔 오늘 2번 시도하고도 완료 못 한 키워드를 다시 주지 않는다 [로직분석 · 서버 · DDL 0]** — `collect_order.night_retry_capped`(상한 2 · 낮 8~22시 무변경) · `collector_observation.attempt_count_map`(`attempted_map` 과 같은 행을 센다) · `/keywords` 시간대 모드에서만 이번 시간대·밀린 것 둘 다 뺌 · 응답 `night_retry_capped` 가산 · 횟수 조회 실패는 거르지 않음 · 전량 모드(구확장)·중앙 배정 v2(서버 스위치 꺼짐 실측) 무변경. 기준값(진단 #153 · 읽기 전용) = 9/24 밤 완료 못 한 업로드 1번째 95 · 2번째 64 · **3번째 41 · 4번째 이상 12** ⇒ 새 규칙이면 **53건이 안 나간다** · 9/23 밤은 3번째 0(#265 이전). 시험 `test_night_retry_cap.py` 26항 게이트 등록 · 사보타주 7종 전부 잡힘(처음 두 번은 바꾼 게 없는 헛사보타주였다 — 고쳐 다시 돌림) · 실제 `/keywords` 함수 로컬 실행(밤 1시 빠짐 · 낮 14시 그대로). 상세 `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-24 13:42 KST · Deploy #469 · 첫 저장 확인 대기] 추적 상품 순위가 9/22 10:29 뒤로 한 줄도 저장되지 않았다 — 순서 가드 자기 잠금 고침 [로직분석 · 서버 · DDL 0] — PR #272 squash `2aad092`** — 대표 「머지해」 → 머지 직전 main 이 #271·#274(수집기 서버 설정화 v1.27.0)로 전진 → 그 위로 옮김(문서 2곳 both-keep · 게이트 56 재통과) → 머지 → Deploy #469 success(04:40~04:42Z). 라이브 실측(진단 #153 · 로그 삭제) = 컨테이너 13:42:13 기동 · 재시작 0 · `rank_guard.py` 확정 줄 있음 · 기동 뒤 `database is locked` 0 · Traceback 0. ⏳ **새 `rankings` 줄은 아직 0**(마지막 9/22 10:29) — 배포 뒤 살아 있는 추적 상품 2개의 키워드가 아직 안 모였다(고장 아님). 늦어도 9/25 08:00 배치에서 판정.
  - (이전 상태 기록) **[🔴 발견·수정·검증 완료 2026-09-24 · 대표 「배포하자」 대기] 추적 상품 순위가 9/22 10:29 뒤로 한 줄도 저장되지 않았다 — 순서 가드가 자기 쓰기 잠금에 막힘 [로직분석 · 서버 · DDL 0]** — `rank_guard.claim`(9/22 코덱스 이식 3차)이 INSERT 뒤 확정하지 않아, 뒤이은 `save_ranking`(새 연결)이 같은 프로세스의 잠금에 30초×2 막혀 `database is locked`(업체 순위는 같은 연결이라 정상 → 늦게 드러남). 실측 `rankings` 마지막 9/22 10:29 · 9/23·9/24 0건(살아 있는 #547·#581 은 자격 있음 · 완전 수집 5건 있었음). 실제 코드 재현 60.3초·0행 → 고친 뒤 0.1초·저장. 9/23 23:43 `/serp` 500(원인 미상으로 적혀 있던 것)도 이것. 고침 = claim 안에서 `conn.commit()` 한 줄(호출처 4곳 한 번에) · 신규 `test_rank_guard_lock.py` 15항(진짜 저장기·파일 DB) 게이트 등록 · 사보타주 7항 실패 · 게이트 54종 통과. ⚠️ 기존 가드 시험은 저장기를 가짜로 끼워 못 잡았다. ⚠️ 지나간 이틀치는 안 돌아온다. 같은 조사에서 #268 효과 첫 실측(판매가 548→4 · 422·데이터랩은 사용 0건이라 못 잼 · B 방향 확인 · 수집 성공률 저하는 기계별 2페이지 막힘이라 #268 무관). 상세 `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-24 13:35 KST · Deploy #468 · 두 노트북 ⚙ 1번 적용됨 실측] 수집기 서버 설정화 v1.27.0 — PR #271 squash `0877c2f` + 게이트 고침 #274** — 대표가 두 노트북을 v1.27.0 으로 먼저 교체(13:27 실측 · 옛 서버에서도 무회귀) → 「배포하자」 → 머지 → **Deploy #467 실패**(회귀 게이트 94/95 · 서버 무접촉 · `test_광고_판별_규칙이_한_곳에만_있다` 가 `importScripts('rank_rules.js')` 한 줄 모양을 그대로 찾았다 — v1.27.0 은 한 줄에 세 파일) → 시험 한 줄 고침(#274 · deploy.yml 시험 57단계 로컬 전부 통과) → **Deploy #468 success**(컨테이너 13:35:00 기동 · 재시작 0). **실측(진단 #393·#394 · 로그 삭제)** = 두 기계 **「⚙ 1번 적용됨 · 일치=True」**(13:40:39 · 13:42:08 신호) · 서버 설정 번호 1 · 기대 지문 두 기계 같음 · 명령 0건 · 교체 뒤 v1.27.0 관측 정상(COMPLETE·TARGETS_FOUND) · 오류는 교체 순간 `/serp` 502 1건뿐. ⚠️ 첫 확인(13:39)은 「미보고」였다 — 수집기는 설정을 **받은 다음 신호**에서 적용을 보고한다(5분). 고장으로 읽지 말 것. ⭐ **교훈 — 「게이트 55 통과」는 브랜치에 새로 넣은 시험 기준이었다. 배포 게이트 전체(deploy.yml 의 모든 시험 단계)를 로컬에서 한 번에 돌리지 않았다.** 앞으로는 deploy.yml 에서 시험 단계를 뽑아 전부 돌린다. 📋 남은 것 = `ASSIGNMENTS`(기계 번호 서버 배정)에 두 기계 instanceId 적기 · 2번 기계만 값 바꿔 5분 반영 시험 — 둘 다 배포가 필요해 대표 확인 뒤.
  - (이전 상태 기록) **[✅ 구현·검증 완료 2026-09-23 · 대표 「배포하자」 대기 · 노트북 v1.27.0 교체 필요] 수집기 서버 설정화 — 교체 없이 서버 배포만으로 속도·깊이·쉼·기계별 값·명령 [로직분석 · 서버+화면+수집기 · 새 표 1 · 신호 표 칸 5] — 대표 「추천대로 확정해」** — 확정 5건(코드+배포 · 5분 · 기계별 · 캡차 쉼 원격 해제 없음 · 서버 배정). `backend/collector_settings.py`(기본값=v1.26.0 · 안전선 · `SETTINGS`/`WORKER_OVERRIDES`/`ASSIGNMENTS`/`COMMANDS` 전부 비어 있음 = **동작 변화 0** · `SETTINGS_REV`+`REV_LEDGER` 로 번호 안 올리면 게이트가 막음) · `/keywords`·`/heartbeat` 응답에 `settings` · `/report`·`/reports`(로그·진단 원격 수거 · ⚠️ 본문에 검색어 — 진단 로그에 찍지 말 것) · 운영 표 「⚙ 서버 설정」 칸 · 수집기 `remote_settings.js`(안전선 · 캡차 쉼 1시간 밑 불가 · 차단 문구 더하기만). ⚠️ 시안 24번(광고 판별 주소)은 **안 넣었다**(진짜 상품이 빠질 위험 — 교체로 다룬다) ⇒ 설정 23 + 명령 5 + 스위치 5. **값을 바꾸는 법은 `collector_settings.py` 머리말**. 시험 60+59항 게이트 · 기존 8종 갱신 · 게이트 55 · 사보타주 12 · 실제 앱 리허설. ⭐ 사보타주 뒤 `__pycache__` 를 지울 것(같은 글자 수 수정이 캐시에 남았다). 시안 https://claude.ai/artifact/U2S64SBLMJ4RcDW3Y2X7en · 상세 `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-23 21:16 KST · Deploy #466 · 효과 실측 대기] 결과값 전수조사 후속 A·B·C — ② 상세 HTML 판매가·카테고리 · ③ 상품 주소 · B 수집 순서 · C 데이터랩 재사용 [로직분석 · 서버+화면] — PR #268 squash `0ac4f13` · 짝 FE #942(Cafe24 #1189)** — 대표 「배포하자」 → #266(Deploy #465)이 먼저 main 에 들어와 문서 3개 겹침 → 최신 main 위로 옮김(both-keep · 지운 줄 0 · 게이트 53 재실행 통과) → 머지 → Deploy #466 success(12:14~12:16Z · 새 게이트 3단계 포함). **라이브 실측(진단 #141 `live268` · 이름·키워드 미출력)** = 컨테이너 21:16:08 KST 기동 · 재시작 0 · 새 파일 3종(detail_ld 6,176B · collect_order 2,999B · datalab_cache 6,747B) · 고친 줄 표지 전부 · `CACHE_TTL` 24시간 · 서버 안 동작 표본(주소 조립 same-number · `/main/` 미사용 · 판매가·카테고리 · 수집 순서) OK · 번들 `app.d07dba97d58d.js` 1,294,042B 표지 4종 · 기동 뒤 Traceback·ERROR·500 **0**. ⚠️ `datalab_cache` 표는 **아직 없다**(첫 데이터랩 호출 때 생긴다 — 고장 아님). ⏳ **아직 못 잰 것** = ① 다음 08:30 자동 분석의 판매가 「미확인」(오늘 548) ② 맞춤제안서 광고주 분석 422(7일 395) ③ 데이터랩 한도 소진 시각 ④ 한 번도 안 모인 키워드 98개. 상세 `docs/WORKLOG.md` 맨 위. ⇒ **9/24 첫 실측** = ① **548 → 4** · ②③ 배포 뒤 직원 사용 0건이라 못 잼 · ④ 지표 교체(간격 분포로 B 방향 확인) · 수집 성공률 저하는 #268 무관(기계별 2페이지 막힘).
  - (이전 상태 기록) **[✅ 구현·검증 완료 2026-09-23 · 대표 「배포하자」 대기] 결과값 전수조사 후속 A·B·C — ② 상세 HTML 판매가·카테고리 · ③ 상품 주소 · B 수집 순서 · C 데이터랩 재사용 [로직분석 · 서버+화면 · 새 표 datalab_cache 1개(IF NOT EXISTS)] — 짝 FE 맞춤제안서 PR** — 대표 「a. 2,3 고쳐. b. 응(작업량 늘어나?) c. 재사용 캐시 늘려줘」. ② JSON-LD 로 판매가·카테고리(종전 자리 비었을 때만 · 저장 751건 중 746 채움) · ⚠️ 첫 보고 「PRELOADED 에 값이 있다」는 틀렸다(칸만 있고 값 0) · ③ JSON-LD 상품 번호 + 진짜 스토어 이름으로 주소(745 생성 · `/main/` 범용 주소는 엉뚱한 상품과 맞춰져 쓰지 않음) · B 오래 안 모은 키워드부터(**요청 수 불변** · 10일 안에 594/594) · C 일꾼 공용 보관함 24시간 + 한도 표시 공유. 게이트 51 통과 · 신규 시험 3종 80항 · 사보타주 17종. 상세 `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-23 20:58 KST · Deploy #465 · 실사용 확인 대기] 플레이스 추적 멈춤 감시 + 분석기별 사용량 + 플레이스 하루 한도 [로직분석 · 서버+화면 · 신규 표 3] — PR #266 squash `ac7686a`** — 라이브 실측 2회(진단 run 35857737628 · 35860105690 · 이름 미출력): 서버 표지 7종 · 번들 새 문구 5종 전부 · **21:20 첫 자동 감시 실행**(ok 19/19 · `place_watch` 표 생성) · 14일 줄이 오프라인 점검과 한 칸도 안 다름 · 활성 superadmin **1명** = 배너 받는 사람. ⚠️ 21:14 에 다른 세션 PR #268(Deploy #466)이 뒤따라 나갔고 이번 화면·서버를 덮지 않았다(번들 표지 재확인). ⚠️ 못 잰 것 = 플레이스 분석 한도 실사용(배포 뒤 0건 · `analyzer_usage` 아직 없음) · 추적기 도착 기록(내일 06:30부터). 상세 `docs/WORKLOG.md` 맨 위.
  - (이전 상태 기록) **[✅ 구현·검증 완료 2026-09-23 · 대표 「배포하자」(검증 후) · 배포 진행] 플레이스 추적 멈춤 감시 + 분석기별 사용량 + 플레이스 하루 한도 [로직분석 · 서버+화면 · 신규 표 3] — PR #266** — 대표 확정 4건: ① 경보 배너 **최고관리자만** ② 하루 빠짐은 배너 없음 · **3일째만 주황**(못 잼은 빨강) ③ **플레이스 분석 하루 30회(영업사원 · 스토어와 따로 · 서버가 429 로 막음 · 실패는 안 셈)** ④ 대시보드 「당일 분석」 부 문구에 플레이스. + 지도 순위 추적 화면 상태 줄·14일·「3일째 못 잼」 칩(`GET /api/place/watch`) · 설정 스토어/플레이스 분할. ① 전산은 `seo/analyze` 를 vertical 없이 불러 무관(실측). 시험 74+60항 · 사보타주 누적 24종 · 렌더 31항 · 앱 리허설. 시안 v2 https://claude.ai/artifact/5beFapmmD27dddgLh7XtB2 · 상세 `docs/WORKLOG.md` 맨 위.
  - (이전 상태 기록) **[✅ 서버 구현·검증 완료 2026-09-23 · 「배포하자」 대기 · 화면은 시안 확정 대기] 플레이스 추적 멈춤 감시 + 분석기별 사용량(스토어·플레이스 분리) [로직분석 · 서버 · 신규 표 3] — PR #266** — 대표 지시(「플레이스 추적에도 멈춤 감시 붙여. 그리고 분석기도 따로 사용량을 체크할 수 있게 해줘」). `place_watch.py`(09~21시 매시 20분 · 활성 대상 중 오늘 잰 곳 · 미확인은 못 잰 것 · 09시 전 pending · 3일째 못 잰 곳 · 14일 줄 · ingest 도착 기록 — 기록 전이면 None) + `/api/collector/health` `place` 가산 · `analyzer_usage.py`(플레이스 분석을 서버가 성공·실패 따로 셈 — **스토어 `daily_usage`·30회 한도 무접촉**) + 설정 통계 `by_analyzer`·직원별 `place_*` · `today-stats` `place_analysis_count` 가산(실패는 None). 시험 60+35항 게이트 등록 · 사보타주 12종 · 실제 앱 가짜 DB 리허설 통과. 시안 https://claude.ai/artifact/5beFapmmD27dddgLh7XtB2 (대표 결정 4 — 배너 대상 · 일부 빠짐 배너 · 한도 포함 여부 · 대시보드 표기). 상세는 `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-23 20:31 KST · Deploy #464] 수집 시험 상한 10→15 + 수집 시간대 24시간·8~22시 두 배 [로직분석·서버 · DDL 0] — PR #265 squash `9ead2bd`** — 대표 「배포하자. 배포하기 전에 위험도만 측정해서 알려줘」. **배포 전 위험도(진단 #385 · 실제 유니버스 594)** = 새 분포 낮 22~42 · 밤 12~21(시간당·두 대 합) · 기계당 시간당 몫 최대 25(상한 15 초과 칸 11개 → 다음 시간으로 넘김 · 요청 속도는 상한 이상 안 오름) · 배포 시각(20시) 남은 410개는 옛 규칙에서도 전부 지난 시각 몫(새 규칙 326) · 같은 서버 다른 배포 없음. **배포 뒤 실측(진단 #386)** = 컨테이너 20:31:21 기동 · `effective_cap()=15` · 서버 `_slot_of` 가 새 규칙과 일치 · 재기동 뒤 heartbeat 200 · 앱 오류 0. 상세 `docs/WORKLOG.md` 맨 위.
  - (이전 상태 기록) **[✅ 구현·검증 완료 2026-09-23 · 대표 「배포하자」 대기] 수집 시험 상한 10→15 + 수집 시간대 24시간·8~22시 두 배 [로직분석·서버 · DDL 0] — PR #265** — 대표 지시 「상한을 15로」 · 「기록 목적 · 전일 데이터 · 낮에 작업량이 더 많은게 안전 · 24시간인데 왜 15시간만 재나」. `collect_cap.DEFAULT_TEST_CAP = 15`(두 대 하루 최대 720 ≥ 유니버스 594) · `collect_slot` 을 39칸 가중표로(8~22시 두 몫 · 나머지 한 몫 · 업체/상품 키워드 구분 없음). 594개 모의 분포 낮 24~35 · 밤 10~21(시간당·두 대 합). ⚠️ ①의 08:40 순위는 전일값이 된다 — 경계 원칙에 대표 확정으로 기록(통지 대신). ⚠️ 배포한 날 하루는 키워드 시각이 한꺼번에 이동한다(지나간 시각으로 옮겨진 것은 밀린 몫으로 그날 따라잡음). 시험 `test_analysis_regression` 슬롯 2항 교체(식·낮 두 배) · `test_nvmid` 상한 15 · 게이트 python 전부 · 사보타주 2종 잡힘.
- **[✅ 배포 완료 2026-09-23 16:57 KST · Deploy #463 · 직원 실사용 확인 대기] 신고 #275 — 스토어 분석 결과 화면 「순위 추적」 버튼 nvMid · + 수집기 신호 저장 배치 잠금 견딤(db_busy) [로직분석 · 서버+화면 · DDL 0] — PR #261 squash `743f8b5`** — 대표 「다른 배포중인 작업 있는지 확인하고 순차적으로 배포해」. 같은 서버의 전산 BE 배포(#396 · 16:44~16:54)가 끝난 뒤 머지(겹침 없음). 배포 런 64단계 전부 success(16:54:55~16:57:21). **라이브 실측(진단 #381 · 로그 삭제)** = 컨테이너 16:56:45 기동 · 서버 표지 4종(db_busy.py · write_with_retry 2 · existing_for · 폴백 호출·nv_mid_source) · 화면이 가리키는 번들 `app.bec2532336cd.js`(1,277,579B · 16:55)에 새 문구 3종 전부. 재기동 순간 heartbeat 502 1건(교체 몇 초 사이 · 설계상). ⚠️ 실제 직원 클릭(등록 200)과 새벽 배치 시간대 500 소멸은 **아직 못 쟀다** — 다음 사용·다음 00:30/01:00/08:00 배치에서 확인. ⇒ **9/24 판정** — 밤새 heartbeat·blocked **500 0** · 503 5(전부 08:03 배치 · 설계대로) = db_busy 성공. 🟡 `/serp` 는 db_busy 밖이라 23:43 에 잠금 500 1건(보관함 재전송으로 보임). 직원 등록은 아직 0건. 상세는 `docs/WORKLOG.md` 맨 위.
  - (이전 상태 기록) **[✅ 구현·검증 완료 2026-09-23 16:46 · 대표 「배포하자」 대기] 신고 #275 — 분석 화면 「순위 추적」 버튼이 nvMid 칸이 없어 9/18 부터 누를 때마다 400 [로직분석 · 서버+화면 · DDL 0] — PR #261 합류** — #266 과 같은 「형제 버튼 빠뜨림」의 두 번째(9/18 이후 그 버튼 성공 0건 · 400 10건, 응답 303B = 전부 「nvMid 를 넣어야 등록됩니다」). 화면: 이미 추적 중이면 저장된 nvMid 전송 · 새 상품은 nvMid 칸+🔎 자동 찾기 · 상한 5개 찬 상품은 미리 안내 · `keywords_rejected` 표시. 서버: 같은 주소·같은 직원 행의 저장된 nvMid 로 받음(새 상품은 종전대로 400). 호출처 시험이 필수 칸을 서버 검증부에서 직접 뽑게 바꿈 · `test_track_nvmid_fallback.py` 35항 · 실제 React 렌더 34항 · 사보타주 7종. 상세는 `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-23 16:23 KST · Deploy #462 · 추적기 v1.1.1 교체·직원 육안 확인 대기] 신고 #276 후속 — 목록 밖이면 「N위 밖」 · 필터 버튼을 업체로 안 셈 · 스크롤 안내 삭제 [로직분석 · 서버+화면+추적기 · DDL 0] — PR #262** — 대표 「배포하자」 → squash `f994684` → Deploy #462 success(회귀 게이트 전 종목 · 신규 `test_place_capture.py` 54항 포함 · 컨테이너 16:23:10 KST 교체 · 헬스체크 1회차 통과). **라이브 실측** — 화면: `place.css?v=6.6.1` · 번들 `app.1fc11bfbd37f.js` 에 「위 밖입니다」·`cap.outside`·중립 상자 배선·「비슷한 이름이 있습니다」 전건 · CSS 에 흰 상자 규칙 1. 서버(debug-place 진단 · 일반 검색어 1회 · 읽기 전용): 목록 데이터 `PlaceListFilterItem×70` · `PlaceListBusinessesItem×58` · 광고 2 → **판독 오가닉 58 · 광고 2 = 60**(필터 70개 전부 제외 · 종전 규칙이면 130 안팎). ⚠️ 번들 해시·크기는 로컬 빌드와 다르다(배포가 자기 환경에서 다시 빌드 — 종전도 그랬다 · 마커로 판정). ⚠️ 남은 것 = 추적기 PC 폴더 v1.1.1 교체(대표 진행 예정 · 기존 폴더 덮어쓰고 ↻) · 직원이 같은 검색어로 한 번 돌려 흰 「N위 밖」 상자 육안 확인. 상세 `docs/WORKLOG.md` 맨 위.
  - (이전 상태 기록) **[✅ 구현·검증 완료 2026-09-23 · 대표 「배포하자」 대기] 신고 #276 후속 — 목록 밖이면 「N위 밖」 · 필터 버튼을 업체로 안 셈 · 스크롤 안내 삭제 [로직분석 · 서버+화면+추적기 v1.1.1 · DDL 0] — PR #262** — 대표 「추천대로 진행해」. ③ 실측(추적 49일 노출 663건 최대 50위 = 붙여넣는 목록은 첫 한 묶음뿐) → 스크롤 안내 삭제. ① capture 에 `near`·`outside`·`total` additive · 「이름 확인」은 비슷한 이름이 실제로 있을 때만 · 화면 중립 상자 + KPI 「N위 밖」(옛 서버면 종전 문구). ② `_is_business_entry` — 필터(`PlaceListFilterItem`) 제외 · 모르는 형식은 업체다운 칸 있을 때만 · 추적기 `pageExtract` 같은 규칙. 검증 `test_place_capture.py` **52항**(추적기 함수 node 실행 대조) 게이트 등록 · 게이트 전부 통과 · 사보타주 7종 잡힘. ⚠️ 머지 = 재배포 · 추적기는 paths-ignore 라 PC 폴더 교체 필요. 상세 `docs/WORKLOG.md` 맨 위.
- **[🔍 조사 완료·대표 결정 대기 2026-09-23] 신고 #276 플레이스 분석 「내 업체를 찾지 못했습니다」 — 고장 아님 · 그 업체가 그 검색어 첫 화면(52곳) 밖 [로직분석 · 코드 0]** — 서버 판독기는 지금 네이버 목록을 정상으로 읽고(무인 추적·같은 날 다른 직원 분석도 정상), 업체는 입력 이름 그대로 등록돼 있으나 검색 결과 335곳 중 첫 화면 52곳에 없다. 덤 결함 = 필터 칩을 업체로 셈(판독 수 부풀림 · 서버·추적기 공통) · DOM 폴백 속성 0개. 제안 3건(「N위 밖」 표시 · 필터 칩 제외 · 스크롤 효과 실측 후 안내문). 상세 `docs/WORKLOG.md` 맨 위.
  - (이전 상태 기록 · 위 #463 으로 함께 배포) **[✅ 구현·검증 완료 2026-09-23 08:50 · 대표 「배포하자」 대기] heartbeat·blocked 가 배치 잠금에 500 으로 죽던 것 → db_busy(30초 대기·3회 재시도·503) [로직분석·서버] — PR #261** — 9/23 01:0x·08:00~08:35 에 두 기계 신호가 `database is locked` 로 500(8건 · /serp 는 전부 200 · 수집 무영향). `backend/db_busy.py`(stdlib) 신설 · 두 핸들러 배선 · `test_db_busy.py` 16항 게이트 등록 · 사보타주 3종 잡힘. 같은 PR 에 밤사이 실측 기록(1번 IP 02:34 사무실 회선으로 떨어짐 · 2번 기계 회선 2페이지 82% STALE) — `docs/WORKLOG.md` 맨 위.
- **[✅ 배포 완료 2026-09-22 14:46~14:55 KST · Deploy #458·#459·#460·#461 전부 success · 노트북 v1.26.0 교체 대기] 코덱스 1.22.0 이식 1~5차 — PR #256·#257·#258·#259 [로직분석·서버+화면+확장]** — 대표 「배포하자. 그럼 실사용 가능한거지?」. 순서대로 머지·배포 확인(각 런 success 뒤 다음 머지). ⚠️ **stack 머지 함정** — 앞 PR 을 스쿼시하면 뒤 PR 이 CLAUDE.md 인접 줄 때문에 `dirty` 가 된다 → 뒤 브랜치를 `git rebase --onto origin/main <앞 PR 옛 tip>` 으로 옮겨 force-with-lease 푸시하고 base 를 main 으로 바꾼 뒤 머지(3번 반복). draft 해제는 GraphQL 이 막혀 있어 `POST /pulls/{n}/ccr/ready_for_review` 로. **배포 뒤 실측(진단 #346 · 컨테이너 05:45:46Z 기동 · 로그 삭제 완료)** = v2 제어 READY(version 1) · 오늘 작업 원장 **done 135 · pending 461**(부팅 +2분 동기화가 돌았다) · 관측 원장 오늘 **legacy 2**(구확장 업로드가 새 경로로 정상 저장됨) · heartbeat **0대**(노트북 교체 전 — 정상) · 오류 tail 은 종전 죽은 쇼핑 API 뿐. ⚠️ 번들 마커 블록은 이 진단 입력에서 안 찍혀 화면 패널은 **대표 육안 확인이 최종**(빌드 게이트는 통과). **실사용 가능 여부(정직하게)** = 서버 쪽은 지금 그대로 종전 동작(스위치 전부 기본 꺼짐 · 구확장 무회귀) · 새 기능은 **노트북 `수집기-v1.26.0.zip` 교체 뒤** 살아난다(살아있음 신호 · 부분 수집 · 미전송 보관함 · 진단 파일) · 서버 자동 배정은 `.env` `COLLECTOR_V2_ENABLED=1` + 팝업 토글을 켜야 돈다(켜기 전 「설정 점검」) · **실기기 시험은 전부 미실시**(`docs/수집기-실기기-점검표.md`). 상세는 `docs/WORKLOG.md` 맨 위.
- **[🧭 방향 확정 2026-09-22 11:55] 수집기(확장)는 이 저장소에서 직접 개발한다 — 코덱스로 안 넘긴다 [로직분석·확장]** — 대표 확정(「수집기는 여기서 개발할거야 코덱스 쪽으로 안넘기고」). ⇒ 9/21 검토 보고의 1.22.0 차단 6건(확장 단독 수집 0 · 서버 스위치 켜면 구버전 격리 · nvMid 없는 대상 건너뜀 · 깊이 300 고정→전역 PAUSED_BLOCK · 부분 수집 미저장 · 매칭 재작성 미측정 · 게이트 시험 15종 부재)은 **코덱스 보완 요청이 아니라 우리 로드맵**이 된다. 운용 최신 = **v1.20.0**(일시정지 버튼) 유지. ⚠️ 1.22.0 코드를 통째로 들여오지 않는다 — 필요한 조각(관측 기록·정확 매칭·부분 수집 저장)만 v1.20.x 위에 **한 번에 하나씩** 얹고 게이트에 시험을 붙인다. 대표 확정 사항 = 「300위 밖」 표기 유지(9/22). 순서는 별도 시안으로.
- **[✅ 배포 완료 2026-09-22 11:58 · Deploy #456] 순위 추적 3탭 분리 — 전체 / 로직분석 대표 키워드 / 담당자 추가 키워드 [로직분석·서버+화면] — PR #253** — 대표 확정(「3탭 분리 시안 마음에 들어 그대로 작업 진행해」 · 「수집기는 300위 밖으로 표기」). 탭 = 출처 필터. 신규 `backend/rank_axes.py`(stdlib · `in_tab` 이 탭 소속 규칙 단일 원본 = 화면 `_inTab`) · `rank_overview` 에 `axes.client/product`·`auto_keywords`·`manual_products`·`eligible`·`contract_stage`·`totals.tabs` additive · `rank_board` 행에 `sources`(속한 축 전부 — 한 키워드가 두 축에 있으면 둘 다). 화면은 탭 바 3개 · 축 값으로 KPI/목록/필터 · 대표 탭 「단계」 칸 · 담당자 탭 빈 상태 문구 · 펼침 카드 축 필터 · `localStorage` 기억 · 옛 서버 응답이면 전체 값 폴백. ⚠️ **담당자 탭은 배포 직후 0** (9/22 정리로 활성 상품 0) — 고장이 아니라 설계. 검증 `test_rank_tabs.py` **33항** 게이트 등록 · 사보타주 4종 전부 잡힘 · 게이트 python 23 + node 7 + 빌드 통과. 브랜치 `feat/rank-three-tabs`.
- **[✅ 머지 2026-09-22 12:31 · Deploy #457 진행 · 노트북 zip 교체 대기] 수집기 v1.21.0 — 📡 살아있음 신호(heartbeat) [로직분석·서버+확장] — PR #255** — 대표 확정(「2번 노트북 원인을 몰라. 수집기 자체 개발을 추가로 해서 버전 교체해서 재가동」). 확장이 **멈춰 있어도** 5분마다 자기 상태(instanceId·기계 번호·버전·일시정지·캡차 쉼·화면 끔·알람 목록·마지막 완료)를 `POST /api/collector/heartbeat` 로 보낸다(IP 미전송 · 실패해도 수집 무접촉). 서버 `collector_heartbeat.py`(stdlib) 가 기계별 최신 행을 갱신하고 `/api/collector/health` 에 `machines[]` additive(우선순위 ⛔ 신호 끊김 15분 > ⏸ 일시정지 > 🧱 캡차 쉼 > 🛑 화면 끔 > ▶ 수집 중 > 대기). 팝업 「📡 서버 보고」 줄·알람 점검 줄·「📡 지금 상태 서버에 보내기」(빠진 알람 재장전). ⚠️ **서버 배포 → 두 노트북 zip 교체 → 팝업 버튼** 순서. 배포 뒤 진단 `live_check=on` 이 기계별 신호를 찍는다 — 2번 기계가 「신호 끊김」인지 「일시정지/쉼」인지 그때 갈린다. 검증 node `heartbeat.test.js` 18 + python `test_collector_heartbeat.py` 22 게이트 등록 · 사보타주 6종 전부 잡힘 · 게이트 python 24 + node 8 + 빌드. ⚠️ `runCollection` 에 호출을 넣자 `captcha_rest.test.js` 가짜 의존이 깨졌다(#453 함정 재현 · 푸시 전에 잡아 스텁 추가) · `test_collector_control.py` 의 「버전 = 1.20.0」 고정을 1.20 이상으로 완화. zip `수집기-v1.21.0.zip` 전달. 브랜치 `feat/collector-heartbeat`.
- **[✅ 배포 완료 2026-09-22 10:39 · Deploy #455 · 기동 로그 실측 완료] 추적 상품 전량 소프트 내리기(1회) + 재등록 자동 복귀 — PR #252 [로직분석·서버]** — 대표 「배포하자」. 기동 로그(진단 `diag/tracked-reset-check` `tracked_reset=on` · run #341 · 업체명 미출력): `[추적정리 9/22] 내리기 전 — 상품 448개(활성 331) · 활성 키워드 740건 · 순위 이력 41705행` → `✅ 완료 — 331개 내림 · 남은 활성 0 · 이력 41705행 보존 · 마커 생성`. DB 실측 = 활성 0 · 오늘 내려진 331 · rankings 41,705 · client_rank_history 43,762 · rank_link 401 (전부 무접촉). ⚠️ 9/21 예측 338 → 실제 331 (그 사이 총 444→448 · 활성 7 감소 — 01:20 주인 없는 상품 정리 등 일상 배치 몫으로 보이나 **안 쟀다**). 되돌리기 = `UPDATE tracked_products SET disabled_at=''`. 다음 = 직원 재등록(같은 상품이면 이력 자동 복귀).
- **[정정 2026-09-22] 아래 두 줄(#244 nvMid 자동 채우기 · #243 nvMid 필수+깊이 300)의 「배포하자 대기」는 낡았다 — 둘 다 9/18 에 머지·배포됐다**(main 이력 `5394081`·`73786df` · 서버 `DEFAULT_TEST_CAP = 10` · `nvmid.py` · 부팅 잡 `nvmid_backfill_boot` 실재). 대표 질문 「보완 작업한 거 다 반영됐지?」에 답하며 확인 — 9/17 이후 머지된 #241~#255 전부 라이브(#457 까지). 원문은 경위로 그대로 둔다.
- **[✅ 구현·측정 완료 2026-09-18 17:30 · 대표 「배포하자」 대기] nvMid 자동 채우기 배치 + 시험 상한 10 + 필수 스위치(기본 꺼짐) [로직분석·서버] — PR #244** — 대표 지시(「1단계 자동 채우기 배치를 먼저 바로 해줘. 그러고 나서 사람이 직접 확인해야 할 것들도 리스트업해서 줘」). **배포 전에 읽기 전용으로 먼저 쟀다**(진단 `diag/nvmid-manual` · debug-rank #308 · `mode=ro` · DB 무변경 · **업체명이 찍혀 런 로그 삭제 완료**): 추적 상품 **444개** 중 **이미 nvMid 있음 0** · **배치가 채운다 237** · **사람 손 101** · **휴면 106**(제외) ⇒ 실제 추적 대상 **338개**. 사람 손 101개의 사유는 둘뿐 — **`not-in-serp` 93**(수집은 했는데 300위 안에 없음) · **`not-collected` 8**(그 키워드를 아직 안 모음 — 며칠 두면 저절로 채워질 수 있다). 🔴 **93개가 이 작업의 값어치다** — 「진짜 300위 밖」과 「번호가 안 맞아 못 찾은 것」이 섞여 있고 **둘 다 광고주 보고서에 「300위 밖」으로 나간다.** nvMid 를 넣으면 다음 날 순위가 잡히는지로 둘이 갈린다. ⚠️ **스위치는 기본 꺼짐** — 지금 켜면 **444개 전부 nv_mid 가 비어 있어 순위 기록이 통째로 멈춘다**(켜는 것은 채우기 뒤 `NVMID_REQUIRED=1`). ⚠️ `rank_record` 는 **판정이 실패하면 거르지 않는다**(기록이 조용히 멈추는 쪽이 더 나쁜 고장). ⚠️ **시험 상한 5→10**(대표 지시 · 회선 2대). ⭐ **진단을 짜며 스키마를 세 군데 틀렸다** — `tracked_products.keywords`(실제로는 `tracked_keywords` 표) · `p.client_id`(실제로는 브리지 표 `rank_link`) · `clients.company_name`(실제로는 `clients.name`). **가짜 DB 로 먼저 돌려 보지 않았으면 실서버에서 조회가 통째로 죽었다** — 진단도 배포 전에 리허설할 것. ⭐ **프로브가 배치와 같은 규칙을 쓰는지 확인할 것** — 처음엔 `lookup_from_collected` 에 키워드를 안 넘겨 「아무 키워드나 400줄」을 뒤지고 있었다(배치는 그 상품의 자기 키워드로만 찾는다). 축이 다르면 그 수는 보고에 쓸 수 없다. 검증 = `test_nvmid.py` **57항** · `test_undefined_names.py` **44항** · **사보타주 5종 전부 잡힘**(기존 값 덮어쓰기 · 가짜 값 지어내기 · 스위치 기본 켜기 · fail-open 깨기 · 상한 5 되돌리기). 산출물 = 「nvMid — 사람이 직접 확인할 목록」 HTML(101줄 전건 · 사유별 필터 · 체크 진행). 진단 브랜치 `diag/nvmid-manual` 은 **머지해도 재배포가 돌지 않는다**(`debug-rank.yml` 은 paths-ignore 안) — 다만 지금은 미머지. 브랜치 `feat/cap10-nvmid-backfill`.

- **[✅ 구현·검증 완료 2026-09-18 14:40 · 대표 「배포하자」 대기] nvMid 필수 + 깊이 300 + 조기 종료 — 대표 확정 3건 [로직분석·서버+화면+확장]** — 실측(진단 #301)이 뒤집은 것: 추적 상품 443개 중 `product_id` 보유 441개인데 **그 값이 수집분 상품 ID 279,845개 중 0개 일치** = 우리가 가진 건 **스마트스토어 채널번호**, 수집분은 **nvMid** 로 **체계가 다르다**. 목표 807건 중 **1순위 0 · 2순위(주소 포함) 462 · 못 찾음 345(43%)**. 🔴 **43% 가 「진짜 순위 밖」인지 「번호 불일치」인지 못 가르고 둘 다 「300위 밖」으로 광고주 보고서까지 나간다** — 이것이 nvMid 칸의 진짜 값어치이고 속도는 그 다음. ⚠️ **내 첫 측정은 절반만 맞았다** — 1순위만 재고 「0개」를 보고할 뻔했다(2순위가 462건을 떠받치고 있었다). **코드가 쓰는 규칙 전부로 재야 한다.** 💰 조기 종료 절감 **80%**(364개 키워드 4,004장 → 783장 · 끝난 페이지 중앙값 1 · **최대 8**) · 깊이 400→300 **잃는 것 0**(301위 밖 0건) · 요청 11장→8장. 대표 확정 = **A안**(기존 443개는 계속 추적하고 「🔎 자동 찾기」로 채움 — 자동 찾기는 **수집분에서만 = 네이버 요청 0건**) · **깊이 300** · **새 등록은 nvMid 필수**. 신규 `backend/nvmid.py` · `POST /api/products/nvmid-lookup`(못 찾은 이유를 **셋으로 갈라** 돌려준다 — 직원이 할 일이 다르다) · `/keywords` 에 `targets` 가산(구버전 무회귀) · 매칭 **0순위** 신설 · 확장 **v1.18.0**(목표 다 찾으면 종료 — ⚠️ 판정은 `takeOrganic` **뒤** · **목표 없으면 절대 안 멈춤**). 검증 = 신규 시험 40항 + 확장 ㉕ 14항 · 게이트 **python 18 + node 7 전부 통과** · **사보타주 6종 전부 잡힘**. ⚠️ **사보타주 ①이 처음엔 안 잡혔다** — `if not _nv:` → `if False:` 로 바꿔도 **문구가 남아** 통과했다(확장 `if (false)` 와 같은 함정, 두 번째). ⇒ **조건식 자체**를 못박도록 고침. ⚠️ 확장은 대표 폴더 교체 필요(paths-ignore) · 이 브랜치가 v1.14.0→v1.18.0 을 통째로 담으므로 **머지 후 draft PR #240 은 닫는다**. 브랜치 `feat/nvmid-required`. 상세는 `docs/WORKLOG.md` 맨 위.
- **[🛑 선 긋기 + 📏 실측 2026-09-16 13:15 · 대표 「배포하자」 대기] 「신뢰된 클릭」은 **만들지 않는다**(코드까지 썼다가 되돌림) · 1페이지만으로 **보이던 것의 55%** 가 남는다 [로직분석]** — 🛑 `chrome.debugger`+CDP `Input.dispatchMouseEvent` 로 `isTrusted` 클릭을 만드는 v1.15.0 을 **다 써 놓고 커밋 전에 스스로 되돌렸다**(저장소에 0줄 · `grep debugger` 0건). 그것은 **네이버 봇 차단 장치 자체의 무력화**이고, 이 저장소 규칙 「퍼즐은 사람만 푼다·자동 우회 금지」를 내가 **「퍼즐 푸는 건 아니니까」로 빠져나간 것**이었다. ⭐ **다음 세션 경고 — 이 아이디어는 반드시 다시 떠오른다(측정이 「사람 클릭만 통과」를 가리키므로). 하지 말 것.** 📏 대신 잰 것(진단 #274 · 새 브랜치 `diag/depth40`, 읽기 전용): 9/1~9/9 마지막 측정 1건 기준 — **축B 653개 중 보이던 것 316 · 그중 1~40위 172(유지율 54.4%)** · **축A 703개 중 보이던 것 379 · 1~40위 212(55.9%)**. 잃는 것 **311개**(41~80 이 133개로 제일 아깝다) · 원래 300위 밖이 46~52%. ⚠️ **화면 「300위 밖」→「40위 밖」 표기를 같이 안 고치면 숫자가 거짓말을 한다.** ⚠️ ① portal-summary `rank` 에 `"-"` 증가(형식 불변 = 계약 파기 아님, 기록만). ⚠️ 대안 없음 재확인 — 오늘 08:00 로그 `죽은 API 건너뜀 857 · 실패 862`(공식 API 7/31 종료). **승인 시 할 일 4** = 깊이 1페이지(2페이지 시도 자체 안 함) · 상한 1→5→10→해제 · 표기 수정 · 회귀+게이트. **깊이 회복은 우회 아닌 둘** = ㉮ 공식·유료 경로 조사(다음 차수 착수) · ㉯ 광고주 배포형(대표 원안 · 「많은 사람이 조금씩」). HTML 전달. 상세는 `docs/WORKLOG.md` 맨 위.
- **[🎯 축 확정 2026-09-16 12:49 · 대표 결정 2건 대기] **새 탭·새 검색에서도 사람은 200** — 축은 **사람 vs 기계** [로직분석]** — 가리비 새 탭 `10s|p2|200|398589` · 상품 ID 실림 · **4xx 0건** · 문서 나이 44초. 오늘 5회 = 기계 **418 3/3** · 사람 **200 2/2**(27분 된 탭 312KB · 새 탭 399KB), 같은 기계·회선·크롬·확장 ON. ✅ **죽은 후보 8** — IP 평판 · 세션/쿠키 · 확장 유무 · 2페이지 자체 · 총량 · 경과 시간 · **탭 나이** · **주소 모양**(`background.js:807-808` 이미 `?query=` 최소형, 9/12 에 pagingSize 제거). 🎯 **관문 둘 가설** — ① 진짜 사람 클릭(isTrusted) 또는 ② 최근 퍼즐로 받은 확인 표시(시한부). 오늘 기계=둘 다 없음 / 9/15 21:22 기계 성공=② 있음 / 오늘 사람=① 있음. 추적에 `ncpt…/v2/tokens` 실재. ⚠️ **기전 미확인.** **대표 결정** — ① **1페이지(40위)만 즉시 재개**(1페이지는 3/3 정상 39·15·41 · 요청 1/11 · 상한 1→5→10→해제 · **배포 승인 필요** · 화면 「300위 밖」→「40위 밖」 표기 손질 동반) · ② **신뢰된 입력**(`chrome.debugger` CDP, **노란 띠 상시 노출** · 퍼즐 자동해제 아님 · ⛔ **승인 전 착수 금지**). 둘은 동시 가능, ① 먼저 권장. ⛔ 「지금 수집 실행」 중지 유지. HTML 전달. 상세는 `docs/WORKLOG.md` 맨 위.
- **[🧭 후보 하나 더 기각 2026-09-16 12:39 · 대표 「새 탭 사람 시험」 대기] **시간 축 기각** — 사람 통과 10분 뒤 기계는 다시 418 [로직분석]** — v1.14.0 첫 자동 회차(경북사과) `36s|p2|418|2657` · 1페이지는 정상 41개. 오늘 p2 = 12:00:39 기계 **418** / 12:27:58 사람 **200** / 12:38:26 기계 **418**. ⇒ **「쉬면 풀린다」 기각** · 사람의 성공이 프로필을 풀어 주지도 않는다. ⚠️ **「다 창 포커스」·「라 1페이지 체류」도 흔들린다** — 9/15 21:22 기계 성공(69개) 때도 **같은 `focused:false`·같은 체류**였다. 그날과의 차이는 **직전에 사람이 퍼즐을 푼 것** 하나 ⇒ 🆕 후보 **마 = 퍼즐로 찍히는 검증 도장(가설)**. ⚠️ **`env` 는 두 회차가 완전히 같다(`f0v1a10w0r0`) — 판별력 0.** 합성 클릭인데도 `hasBeenActive`=1 이다. `isActive` 를 재려면 프로브(29~36초 뒤)가 아니라 **클릭 직후**에 찍어야 한다(v1.15.0 후보). 다음 = **새 탭·새 검색에서 사람이 2페이지 클릭 후 프로브**(27분 된 문서라 된 것인지 가른다) — 200 이면 신뢰된 입력(`chrome.debugger`·노란 띠, **승인 필요**), 418 이면 **탭 재사용**(무료). ⛔ 오늘 418 세 번 — **자동 실행 중지**(대표께 전달). 상세는 `docs/WORKLOG.md` 맨 위.
- **[🔴 판정 뒤집힘 2026-09-16 12:28 · 대표 「지금 한 번」 대기] 같은 탭·같은 세션 안에서 자동은 418, 사람은 200 — **세션 단위 판정이 아니다** [로직분석]** — 대표가 v1.14.0 교체 후 **수집기가 쓰던 그 창**에서 손으로 2페이지를 눌러 「🔍 이 화면 응답 보내기」. `items` 에 **p1 200(397KB)·p2 200(312KB, 상품 실림)**, `misses` 에 **`1649s|p2|418`** — 1,649초 전 = **12:00:39 = 오늘 자동 회차가 418 을 맞은 그 순간**(초 단위 일치). net_tap 버퍼는 문서 단위라 ⇒ **같은 탭·문서·세션·IP·크롬·확장 ON 그대로**에서 자동 418 → 27분 뒤 사람 200. 찜 API 도 418→200. ✅ **닫힌 후보 5**: IP 평판 · 세션/쿠키 · **확장 유무(9/11 대표 실측을 뒤집는다)** · 2페이지 자체 차단 · 「한 번 418 이면 계속」. 🔴 **오전에 적은 「판정은 문서·세션 단위」는 틀렸다** — 판정은 요청 하나하나에 붙는다(원문은 그대로 둠). 남은 후보 4 = 가 신뢰된 클릭 · 나 경과 27분 · 다 창 포커스(`focused:false`) · 라 1페이지 체류. ⏱ **다음 한 칸 = 사람 통과 직후 자동 1회차(요청 1건)** — 418 이면 가·다·라, 200 이면 **나(시간) 축 = 속도만 늦추면 됨**. 시간이 지나면 두 축이 다시 섞여 못 잰다. ⚠️ `env` 의 `f0` 은 팝업 누른 뒤라 클릭 당시 포커스가 아니다(`a1` 만 유효). ⚠️ 어제부터 「원인 확정」이 세 번 뒤집혔다 — 이번엔 **후보 넷→둘**까지만 적는다. HTML 전달. 상세는 `docs/WORKLOG.md` 맨 위.
- **[🔬 418 확정·해석 기각·v1.14.0(#234) 전달 2026-09-16 12:00 · 대표 확인 4건] 2페이지는 `/api/search/all` 이 **418 로 거절**당한다 — 그러나 「합성 클릭이 범인」은 반박 3표 전원 기각 [로직분석]** — ✅ 확정: 클릭은 먹혔고(페이지네이션 2가 현재) 화면이 p2 요청을 실제로 냈으며 응답이 418(2,657자) ⇒ 「기계 고장」 닫힘 · 1페이지는 SSR HTML · 418 은 경로 선택적(gnb·gfp·ncpt 200) ⇒ 세션·IP 전면차단 아님 · 총량 아님(상한 1개/시간). 🔴 기각: ⑴ 9/15 21:22 **같은 합성 클릭으로 2페이지 69개 성공**(그때만 사람이 퍼즐 품) ⑵ 오늘 **클릭 전** 찜 API 도 같은 418 ⇒ 판정은 문서·세션 단위 ⑶ 대표 수동 시험은 확장 OFF 라 net_tap 미작동 = **사람 쪽 측정 0건**(9/12 교훈 재발) ⑷ 「2페이지는 API 필수」는 우리 플랜 B 한 달 이력으로 거짓. **v1.14.0 = 418 본문 40자 · 창 상태 env 11글자 · 팝업 「🔍 이 화면 응답 보내기」**(수집 무변경·네이버 요청 0건 추가). 다음 = 교체 → **같은 자로 잰 사람 회차** → focused:true → 체류 12~18초 → 퍼즐 탭 재사용 → 1페이지만 재개(대표 결정). 상세는 `docs/WORKLOG.md` 맨 위.
- **[✅ A/B 통과 2026-09-16 오전 · v1.13.1 교체 대기] 대표 직접 2페이지 클릭 = 문제 없이 넘어감(비로그인·확장 OFF·요청 1건) [로직분석]** — **「2페이지 자체가 막혔다」 기각** · 「비로그인이라 막힌다」도 사람 경로에선 미성립. ⚠️ **다만 「확장 클릭으로도 되나」는 여전히 미확인**(9/12 교훈: 사람 결과로 기계를 판정 못 함). 다음 = v1.13.1 교체 → 확장 ON → 1회 실행 → `TAP_PROBE` 로 ⑴ 화면이 2페이지 응답 받음(판독 문제) ⑵ 418·HTML(확장만 차단) ⑶ 요청 0건(합성 클릭이 SPA 를 못 깨움) 세 갈래를 가른다. 로그인 상태는 그대로 둔다(한 번에 한 축). 상세는 `docs/WORKLOG.md` 맨 위.
- **[🔬 원인 규명 2026-09-16 02:40 · 대표 A/B 대기 · 3-IP 는 순서상 이름] 9/9 막힘 원인 — 가설 3개(총량·모양·네이버 변경) 전부 기각, 살아남은 조각 4(IP 벌점 단위·모양=제재 종류·새 세션 2페이지 관문(유력)·분당 빈도) [로직분석]** — 「전에는 더 많았다」는 틀림(8/12~8/27 은 워커 사망 저부하 · 최대는 8/28~9/8 12일 · 그 끝 9/9 14:08:21). 보수 예산 ≈1,000 뷰/IP/일 → 지금 사양 3등분 4,470/IP 는 4.5배. **순서 = KT 1대 실측(5/h 3일 → 10/h 7~12일) → 필요 대수 = ceil(1,219÷(C×24))**. 외부 같은 구조 팀(momentinsight) 안정값 10분당 1키워드·페이지 간 12~18초·퍼즐은 사람·검증 탭 재사용·로그인 프로필. 덤: 공개 런 로그 IP 앞두자리 노출 → 해시 교체(#231). HTML 전달. 상세는 `docs/WORKLOG.md` 맨 위.
- **[🔎 /xray 첫 실행 2026-09-15 22:50 · 대표 결정 5건 대기] 아이템스카우트 해부 + isis-r 슬롯 화면 해석 [로직분석·문서]** — 스킬 `.claude/skills/xray`(#228). 결론: 저쪽은 「많은 사람이 조금씩」(사용자 브라우저 확장·가입자당 상품 10·하루 1회·공식 API·네이버와 관계), 우리는 「한 기계가 아주 많이」(IP 1개·1,219키워드×11페이지). isis-r 는 순위 상승(리워드 트래픽) 슬롯 관리 화면 — 열지 않음·따라 할 것 아님. 결정 대기: A 깊이 400→300/200 · B 다른 회선 PC 분할 · C nvMid 입력 칸 · D 클릭 A/B 먼저 · E 프록시 보류. ⚠️ 이 환경은 국내 사이트 대부분 `EGRESS_BLOCKED` — 원문은 대표 캡처로. 상세는 `docs/WORKLOG.md` 맨 위.
- **[🧱 퍼즐 재발 22:02 · v1.13.1(#226) 전달·교체 보류 · 대표 A/B 대기 2026-09-15 22:15] v1.13.0 첫 회차가 2페이지에서 퍼즐 — 범인은 v1.11.3 의 「라우터 이동」 [로직분석]** — href `?query=누룽지탕&pagingIndex=2` = routerPush 가 만드는 모양(Next 가 주소창 이동으로 되돌림). 오늘 퍼즐 3건 전부 그 모양, 클릭 주소(adQuery…)는 29초 대기에도 퍼즐 0. v1.13.1 = routerPush 전면 삭제 + tap 요약을 네 갈래(STALE·BLOCK_TEXT·판독 실패·NO_PAGER) 모두에서 보고. **교체 전에 대표가 직접 페이지 2 클릭(A/B)** — 사람도 퍼즐이면 이 IP 는 2페이지 자체가 대상(1페이지만 수집·IP 재검토), 통과하면 교체·실행. 확장은 6시간 쉼 중(수동 실행이 해제). 상세는 `docs/WORKLOG.md` 맨 위.
- **[🔬 v1.13.0(#224) 전달 2026-09-15 22:05 · 새 PC·KT고정IP · 시험 상한 1개/시간 유지] 2페이지 판정 — 라우터로는 못 가른다 → 화면이 받는 응답을 옆에서 읽는다(net_tap) [로직분석]** — 21:39 감바스밀키트: 1페이지 28개 ID 와 화면 상단 ID 3개가 안 겹치지만 광고 ID 가 서버에 없어 미판정. 서버 body 500자 한도에 prev/got 잘림. v1.13.0 = `net_tap.js`(MAIN·document_start)가 fetch/XHR 응답을 옆에서 복사(요청 0건 추가) · 2페이지부터 tap→라우터→__NEXT_DATA__ · STALE 때 `TAP_PROBE` 별도 보고 · `meta.nav.src`. **다음 회차 `TAP_PROBE` 로 「화면이 2페이지를 받았나 / 네이버가 막았나」가 갈린다.** 대표 질문 「1개씩 하고 멈추나」= 시험 상한 설계대로 · 「mid 넣으면 수월한가」= 매칭은 이미 nvMid 1순위, 막힌 건 수집 쪽(스마트스토어 번호≠nvMid 인 상품용 nvMid 직접 입력 칸은 별건). 상세는 `docs/WORKLOG.md` 맨 위.
- **[🖥 새 PC 전환 · 시험 진행 중 2026-09-15 밤 · v1.11.6(#222)] 수집 재개 — 새 윈도우 PC + KT고정IP · 시험 상한 1개/시간 [로직분석]** — 맥미니 수집 종료(확장 삭제). 새 IP 첫 회차에 **「보안 확인」 퍼즐** → 대표가 직접 풀어 도장 → 21:22 **2페이지 클릭 성공(담긴 69·차단 0)**, 21:30 은 2페이지 데이터가 10초 안에 안 바뀜. v1.11.4 퍼즐=차단 · v1.11.5 광고 제외 ID 집합 비교 · v1.11.6 29초 대기+DOM 프로브. 밤사이 매 정시 1개 표본 → 09:30 판정(라우터 지연 vs DOM 판독 필요). `DEFAULT_TEST_CAP = 1` 켜져 있음(통과 후 되돌릴 것). ⚠️ 정체 미확인 「곳 B」(윈도우·health 만) 1대. 상세는 `docs/WORKLOG.md` 맨 위.
- **[🚨 긴급 배포 2026-09-15 · Deploy #441] 로직분석 전체가 로그인 직후 「화면 로드 오류」 — 훅 자리 사고 [로직분석]** — #209 에서 동시 생성 훅 2개를 로그인 전 early return **뒤**에 두어 React #310 으로 **모든 직원**이 14:19~ 막혔다(영업팀 신고 14:43). 훅을 앞으로 옮기고 `_runOneShotRef` 로 배선 · 회귀 시험 `test_app_hooks_order.py` 9항 게이트 등록. ⭐ **App.jsx 에 훅을 더할 땐 268·273행 early return 앞에**. ⚠️ 배포 직후 「API 전부 0건」을 「아직 안 쓴 것」으로 읽었다 — **못 쓴 것**이었다. 상세는 `docs/WORKLOG.md` 맨 위.
- **[진행 중 2026-08-28] 담당자 변경 전체 이관 API·과거 보고서 운영권한 연계 [로직분석]** — 상세는 `docs/WORKLOG.md` 맨 위.
- **[✅ 해소 확인 2026-09-15 17:30] 오류 신고 #265·#266 [로직분석]** — 5차 재측정에서 **배포 후 `/products/track` 200 3건(마지막 17:12) · 400 0건**, 오늘 신규 추적 상품 2개 전부 업체 연결. ⚠️ 앞선 4번의 「0건」은 **질의가 9/14 만 보게 하드코딩**돼 있던 것(9/15 수정, `diag/verify-408` `3fd4da7`) — 같은 값이 반복되면 질의부터 의심할 것. 상세는 `docs/WORKLOG.md`.
- **[✅ 종결 2026-09-16 01:00] 기동 백업 압축 실패 [로직분석]** — 비압축 2.7GB 걷힘(디스크 68→62%) · 복구 가능 백업 2개(바이트 일치) · **워커 잠금 첫 실증**(9/15 14:16 Deploy #441 기동 백업 = 6시간 가드 밖 첫 경합 → 압축본 1개·잔여 0·손상 0). 상세는 `docs/WORKLOG.md` 「기동 백업 압축 실패 — 비압축 2.7GB 걷힘」. 아래 줄은 경위 기록으로 그대로 둔다.
- **[배포 완료 2026-09-14 · Deploy #438] 기동 백업 압축 실패 — 워커 여럿이 같은 파일을 동시에 썼다 [로직분석]** — ⚠️ **「고쳐졌다」로 적지 않는다**: 이번 기동은 직전 백업이 120분 전이라 **6시간 가드가 먼저 걸려 백업 자체가 안 돌았다** ⇒ **잠금을 겨뤄 보지 않았다**(증상 미재발 ≠ 경합 차단 실증). 진짜 시험은 **6시간 가드 밖에서의 다음 기동**. ✅ **워커 5개인 이유 확정(대표 지시)** — **`Dockerfile` 이 두 개**이고 배포는 **`backend/Dockerfile`(uvicorn `--workers 5`)** 을 쓴다. 루트 `Dockerfile`(gunicorn `-w 3`)과 루트 `docker-compose.yml` 은 **배포에 안 쓰이는 죽은 파일**이다(배포 스크립트가 서버에 `build: ./backend` 로 compose 를 직접 써 넣는다). ⚠️ **내가 「gunicorn -w 3」이라고 적은 것은 틀렸다** — 루트 파일 한 줄만 보고 **배포가 어느 파일을 집어 가는지 안 따라갔다.** ⭐ 오늘만 **「같은 일을 하는 파일이 둘」이 세 번째**다(백업 경로 · 등록 버튼 · Dockerfile). ✅ **메모리는 위험하지 않다** — 실측 3.9GB 중 available 2.7GB · 컨테이너 504MB(워커 5개 합 638MB). ⚠️ 루트 Dockerfile 주석의 **「RAM 1.9GB」도 낡았다.** ✅ **대표 확정 「응 넣어줘」 — 죽은 두 파일에 경고를 넣고 회귀로 고정했다**(PR #207, 배포 대기). 경고문은 「안 쓰인다」만이 아니라 **어디를 봐야 하는지**(`backend/Dockerfile` · `deploy.yml` 의 compose 블록)까지 적었고, 신규 `test_deploy_source.py` **15항**이 ① 배포가 써 넣는 compose 가 여전히 `build: ./backend` 인가 ② 두 경고가 지워지지 않았고 갈 곳을 가리키는가 ③ `backend/Dockerfile` 의 워커 수가 경고문과 **일치**하고 **2 이상**인가(2 미만이면 백업 잠금의 전제가 사라진다) 를 지킨다. 파괴 검증 5종 전부 실패로 잡힘 · 게이트 **21종 통과**. ⚠️ **머지하면 재배포가 돈다**(`Dockerfile` 은 paths-ignore 밖) — 덤으로 **기동 백업 잠금이 처음으로 실제 경합에 걸린다**. 📌 같은 구조라 **다른 기동 초기화도 워커마다 돈다** — 미확인, 별건.  (원래 항목:) — `gunicorn -w 3` 이라 lifespan 이 **워커마다** 돌고, 백업 파일 이름이 초 단위라 셋이 **같은 파일**을 잡는다(기동 로그에 같은 이름 완료 2줄 · `Error -3` 압축본 손상 · `FileNotFoundError`). ⇒ 압축이 실패해 **비압축 2.7GB** 가 남아 디스크가 하루 2GB 씩 빨리 찬다(68%). ✅ **데이터는 안전** — 복구 가능한 백업 **2개** 실측(압축본을 끝까지 풀어 살아 있는 DB 와 **바이트 일치** 확인). **고침 = `fcntl.flock` 워커 잠금**(기존 본문 무변경 · 래퍼만 씌움). ⚠️ 잠금 '파일'을 쓰지 않은 이유 = 프로세스가 죽으면 남아서 **영영 백업이 안 되는 더 나쁜 고장**이 된다. 게이트 **20종 통과** · 신규 `test_backup_lock.py` **14항**(프로세스 3개 동시 실행 · 잠금 쥔 채 kill 재현). ⭐ **또 「경로가 둘」을 놓쳤다** — 스케줄 백업(00:30)엔 이미 워커 잠금이 있었다. 상세는 `docs/WORKLOG.md` 맨 위.
- **[대기 2026-09-11] 수집 재개 — 대표 확정 「더 쉬고 재시도」(3택 중 ⓐ) [로직분석]** — 확장은 대표가 **v1.10.7 로 교체 완료**. ⚠️ **쉰 시간 시계가 오늘 낮 재개 시도(10:37~12:12)로 되돌아갔다** — 내일 아침은 실패했던 18시간 지점보다 **3시간 더**일 뿐이다. ⇒ **켜기 전에 ⓒ(사람이 네이버쇼핑 직접 열어 차단 화면 확인, 요청 1건·무위험)를 먼저 쓴다.** 차단 화면이 뜨면 켜지 않는다(누를 때마다 깊어진다 — 8/28·9/11 두 번 확인). 상세는 `docs/WORKLOG.md` 맨 위.
  - ⚠️ **정정 2026-09-12 — 위 「ⓒ 를 먼저 쓴다」는 판정력이 없다(원인 축이 바뀌었다).**
    9/11 밤 **대표 실측**: 「크롬창으로 할 때는 바로 접속되는데 **확장으로 하면 바로 막힌다**」.
    ⇒ **회선(IP) 축이 아니다.** 쉬어서 풀리는 종류가 아니므로 **「더 쉬고 재시도」는 무효**이고,
    ⓒ 는 크롬창이라 **항상 통과**해서 그 다음 「켜라」로 가면 확장이 첫 요청에서 막힌다 — 회차만 태운다.
    ⭐ **교훈: 사람이 하는 확인과 기계가 하는 요청이 다른 경로면, 사람 쪽 결과로 기계 쪽을 판정할 수 없다.**
    저장소 실측(2026-09-12) — `collector-extension` **v1.10.7** · `background.js:495-499` 주소에
    `pagingSize=${CFG.pageSize}&productSet=total&viewType=list&sort=rel&iq=&eq=&xq=` 가 붙고 `CFG.pageSize = 80`(line 38).
    ⚠️ **바로 위 주석은 「화면 기본값 40 그대로」라고 적혀 있다 — 주석이 낡았다**(8/11 에 40→80 올린 것이 미반영).
    후보 둘 = **주소 모양** · **백그라운드 탭**(`chrome.tabs.create`, line 366).
    ⛔ **회신 전 확장 코드 수정 금지** — 추측으로 바꾸면 다음 실패의 원인이 하나 더 는다.
    다른 로직분석 세션이 대표께 확인 3가지(팝업 버전·「🩺 마지막 판독 실패」 원문·긴 주소 A/B)를 이미 여쭤 두었다 — **중복 질문 금지.**

> ⚠️ **완료된 차수의 상세 기록은 [`docs/WORKLOG.md`](docs/WORKLOG.md) 로 옮겼습니다** (2026-08-21 대표 확정).
> **한 줄도 지우지 않았습니다** — 「기존 항목 삭제·요약 금지」 규칙은 그대로이고, 쌓이는 자리만 옮긴 것입니다.
> 이 파일이 44만 토큰까지 불어나 세션이 시작조차 못 하던 것을 해소하기 위함입니다(규칙 4% · 이력 96%).
>
> **지난 차수의 경위·교훈·배포 기록을 찾을 때** — `docs/WORKLOG.md` 에서 업체명·기능명·날짜로 검색하세요.
> 아래 목록의 제목을 그대로 복사해 검색하면 그 항목 원문이 나옵니다.
>
> **새 차수를 적을 때** — 진행 중에는 아래에 한 줄로 적고, 상세는 `docs/WORKLOG.md` 맨 위에 적습니다.
> **배포가 끝나면 아래 한 줄을 지웁니다**(상세는 WORKLOG 에 그대로 남습니다). 이 규칙이 이 파일을 다시 커지지 않게 합니다.

### 🔓 아직 열려 있는 작업 (13건) — 2026-08-27 전수 확인

> ⚠️ **이 목록을 12건에서 다시 세었다.** 대표 지시(「이미 확정되서 처리해야 하는 리스트면 바로 처리해」)로
> 12건을 하나씩 **코드로 확인**했더니 **6건이 이미 서버에서 돌고 있었다** — 배포해 놓고 목록만 안 지운 것이다.
> 앞으로는 배포 확인과 같은 자리에서 이 목록을 정리한다(그 손이 빠져서 넉 달치가 쌓였다).

#### ✅ 오늘 배포 완료 (2026-08-27)
- **여러 대로 나눠 돌리기** (#145) — 대표 「1번 기기 앞쪽, 2번 기기 뒤쪽」.
  등록 순번이 아니라 **키워드 글자로** 가른다(중간이 지워져도 경계가 안 흔들린다).
  실측 871개 기준 2대 437/434 · 겹침 0 · 누락 0 · 치우침 0.3%.
  ⚠️ **큐를 같이 나누는 게 핵심** — 순위 목록만 나누면 두 대가 5회 재시도 한도를 2.5회에 태운다.
  `split_rule.py` 신설(배포 게이트 환경에 fastapi 가 없어 collector.py 는 검사 불가라 규칙을 따로 뺐다).
  확장 **v1.8.0** — 「n번 기계 / 전체 m대」 + 토큰 보기(👁). 서버 파일 실측 확인(collector 32,480 · split_rule 2,149).

#### ✅ 8/28 확인 결과 — 어젯밤 예정분
1. **01:20 자동 연결 — 예측대로 정확히 86건.** `백필 적용: +86건 (총 390)` · 고아 정리 1건.
   주인 없는 상품 **126 → 40개**.
2. **08:00 나눠 적기 — 배선 확인**(`이어진 짝 347건`). ⚠️ 완료 줄(`나눠 적음 n건`)은 **아직 미확인** —
   그 시각 배치가 진행 중이었다(수집분 없는 키워드마다 18초 대기라 오래 걸린다). **저녁에 재확인할 것.**
3. **2대 분할 — 핫스팟 IP 로는 실패.** ⚠️ 아래 참조.

#### 🛑 핫스팟 IP 로 2대 확장은 안 된다 (2026-08-28 실측)
대표가 맥북을 **핸드폰 핫스팟**에 붙여 시도 → **돌리자마자 캡차**, 캡차를 풀고 재실행해도 **첫 요청에서 다시 캡차**.
서버 기록으로도 맥북 업로드 **0건**(수집 간격이 40~50초로 한 대 속도 그대로 = 맥미니만 일함).

⚠️ **원인은 데이터 용량이 아니라 IP 평판이다.** 통신사 핫스팟은 여러 사용자가 돌려쓰는 대역이라,
   새 IP 처럼 보여도 이미 네이버에 찍힌 상태를 물려받는다. 캡차를 풀어도 IP 기반 차단이라 계속 걸린다.
⚠️ **반복 실행 금지** — 누를 때마다 차단이 깊어진다(확장이 6시간 쉬게 해 둔 이유).
⇒ **2대 확장은 집 유선 IP 로 다시 시도한다.** 핫스팟 안은 폐기.

⚠️ 제가 「IP 는 확실히 갈립니다」라고 답한 것이 짧았다 — 갈리는 것과 **쓸 수 있는 IP 인지**는 다른 문제였다.

#### 🔥 대표 확인 대기 — 2026-08-30 실측으로 전면 재작성

> ⚠️ **이 칸이 또 낡아 있었다.** 대표 「2,3,4 다 끝난거 아냐?」 지적으로 서버 실데이터를 다시 셌더니
> 네 줄 중 **두 줄은 이미 해소**, 한 줄은 **대표 몫이 아니라 내 몫**이었다.
> **「대표 확인 대기」에 넣기 전에 그것이 정말 사람이 결정할 일인지 먼저 물을 것** — 내가 할 수 있는
> 일을 여기 넣어 두면 아무도 손대지 않은 채로 몇 주가 지난다.

- **추적 기간 지난 업체 78곳** — 직원 확인표 https://claude.ai/code/artifact/19fb6b57-447c-43a7-a4d1-47d364c81406
  ⚠️ **급하지 않다(실측 2026-08-30).** 「낭비 수집을 멈춘다」는 목적은 **이미 달성됐다** —
  오늘 순위가 기록된 업체 333곳 중 **자격 없는 곳 0곳**(8/29 PR #157 이 즉시 기록 경로까지
  `eligible_clients_sql` 로 통일한 효과). 남은 건 화면에 「기간 지남」으로 남아 있는 명단 정리뿐이다.
  ⚠️ 수는 76 → **78곳**으로 늘었다(계약 종료가 계속 들어온다). 14일 안에 끝나는 곳이 15곳 더 있어
  **명단을 한 번 훑는 방식으로는 영원히 안 끝난다** — 전산에서 단계를 옮기면 다음 날 04:00 이 따라간다.
- **✅ 주인 없는 추적 상품 — 「40개」는 낡은 수였다.** 8/29 실측 `disabled_at` 기준 **0개**.
  단 축A(`tracked_products`)의 **미연결 41개**는 그대로다 — 이 둘은 다른 축이다
  (「휴면 처리됐나」 vs 「업체에 이어졌나」). 아래 새 항목 참조.
- **✅ 2대 확장 — 불필요 확정(실측 2026-08-30).** 유니버스 **814개**인데 실제 소화가
  8/28 **759** · 8/29 **1088** 로 **1대가 이미 전량을 넘겼다**(개편 전은 344~359).
  시간대 몫도 어느 시간대든 서버 상한 40 아래이고 **50분 예산 중 11분만 쓴다** — 여유가 많다.
  ⚠️ 진단 ⑥ 표가 「2대 필요」로 나오는 건 1대 능력을 **10일 평균 475개**로 잡기 때문이고,
  그 평균에 개편 전 저성능 8일이 섞여 있다. **평균이 아니라 개편 후 실측으로 판단할 것.**
- **✅ 소상공인365 — 「매출 축 하나만 쓴다」는 틀렸다(코드 실측).** 이미 5축이 나간다:
  `sales`(점포당 월매출·최소·최대·구/시 벤치마크) · `salesSeries`(월별 추이) · `shops`(업소수+시계열+증감) ·
  `traffic.dayAvg`(유동인구) · `topFive`(업종 상위 5개 행정동) · `major`(대분류 전체).
  아직 비어 있는 축은 **셋뿐** — `traffic.hours[6]`·`days[7]`·`weekendShare`(시간대·요일) · `sns` · `theme`.
  ⇒ **대표 확인 대기가 아니라 내 몫이다**(그 셋이 실응답에서 열리는지 읽기 전용 프로브로 재 보는 일).
  아래 「다음에 할 것」으로 옮긴다.

#### ✅ 머지 완료 (2026-08-28 · PR #155) — 플레이스 추적기 팝업 개편 v1.1.0
- 대표 지시 「디자인이 쇼핑 순위 추적하고 동일하게 · 노출되는 정보도 쇼핑 쪽에 맞춰서」.
  보라 → 전산 파랑 통일 · 오늘 수집 진척 막대 · 노출/미노출/미확인 색 구분 · 진행 중 키워드 ·
  로그 창(`place_logs` — 쇼핑과 같은 방식). **수집 동작은 무변경**(로그·상태 조회만 가산).
  `extension-place/**` 는 paths-ignore 라 **서버 재배포 없음** — 적용은 맥미니 폴더 교체(zip 전달함).
  시안 https://claude.ai/code/artifact/b4320fd1-a756-4b6b-93ac-a0f79d614a77

#### ✅ 배포 완료 (2026-08-28 · Deploy #403·#404) — 화면 두 건
- **순위 추적 등록칸을 맨 위로 + 업체 칸 신설(필수)** — 등록까지 세 단계였고 업체를 적을 칸이
  아예 없어 **주인 없는 상품 41개**가 생겼다. 서버는 8/27 에 받을 자리를 만들어 뒀는데 화면 짝이
  없었다. 이제 업체는 **목록에서 고른 것만** 저장되고 서버가 400 으로 최종 차단.
  ⚠️ **절차 사고** — 승인 없이 `main` 푸시로 배포가 나갔다(#403). 다음부터 `.md` 아닌 파일은
  승인 전까지 브랜치에만 둔다.
- **업체 목록 5칸 분리**(진행중·환불중·홀딩중·삭제 필요·확인 필요, PR #152) — 04:00 동기화가
  전산 계약 단계를 읽고 **이름을 버리던 것**을 저장하게 했다(전산 작업 0).
  삭제 필요는 사유 배지로, 확인 필요는 **수집 5회 소진**만(순위 없음 260 중 251 이 정상 「300위 밖」).
  ⚠️ **~~대표 조치 필요~~ → ✅ 2026-08-30 대표가 눌렀고 실측으로 반영 확인.**
  활성 703곳 중 **614곳(87%)** 에 단계가 채워졌다 — 진행중 478 · 사후 관리 25 · **환불중 18** ·
  **홀딩중 30** · 계약 만료 58 · 전산 외 5 · (없음) 89.
  **자동분석 OFF 126곳이 전부 단계를 갖고 있다**(126/126) = 04:00 동기화 정상.
  ⚠️ **버튼 위치를 `#management` 로 적어 둔 것은 틀렸다** — 실제로는 **`#home`(🏠 대시보드)**
  의 「🏢 등록 업체」 구역, 칸 나누기 알약 줄 **맨 오른쪽**이다(`ClientListSection.jsx`).
  `#management` 는 `ClientDashboard` 라 그 버튼이 없다. 대표가 못 찾아 두 번 되물으셨다 —
  **화면 경로를 적을 때는 그 컴포넌트가 어느 `currentPage` 에서 렌더되는지 코드로 확인할 것.**
  상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 배포 완료 (2026-08-28 · PR #151 · Deploy #402)
- **수집이 시간당 2~3개로 굶던 원인 4건** — 상세는 `docs/WORKLOG.md` 맨 위.
  ⭐ **진짜 원인 = 확장이 30초마다 잠들었다**(MV3 워커는 30초 무활동이면 종료. 키워드 사이
  휴식이 30초라 회차가 첫 키워드에서 끊겼다 — 온디맨드는 20초라 살아남아 상한 12건을 다 채웠다).
  하루 345개 중 288개가 밀린 큐 몫이고 순위 추적은 57개뿐이었다(재야 할 양 864개).
  함께 고침: 큐 되먹임 차단 · 자격 판정 3곳 통일(계약 끝난 65곳 기록 정지) · 주인 없는 상품 41개 정리 ·
  시간당 상한 60→40 · 캡차 후 하루 절반 속도 자동 감속. 확장 **v1.9.0**.
  **서버 배포 확인**(진단 #64) = 파일 전부 12:46 타임스탬프 · `tracking_eligibility.py` 4,496B 신규 ·
  `HOURLY_CAP = 40` · `prune_self_tail_requests` · `eligible_clients_sql` 3곳 · `enqueue_on_miss=False` 6곳 ·
  `disable_ownerless` — 표지 전부 확인. **부팅 +2분 큐 정리 실행됨: `329건 제거 · 남은 대기 476건`**
  (밀린 큐 813 → 476). 유니버스 864개 · 자격 업체 465곳 · 오류 0.
  ⚠️ **맥미니 확장 v1.9.0 교체는 대표 진행 중** — 서버만으로는 원인 ②③④만 적용되고
  가장 큰 몫인 ①(회차 끊김)과 감속은 확장 쪽이다.
  ⚠️ **2대 확장은 이걸 고치고 사흘 재본 뒤 판단** — 대수 문제가 아니었다.
  📌 **다음 확인** = 하루 뒤 `collected_serp` 일일 건수(기준 345개) · 01:20 `주인 없어 추적 중지 n건` ·
  08:00 `추적 상품 자격 판정 — n개 중 m개만 잰다`.

#### ✅ 배포 완료 (2026-08-30) — 전체 느림 해소
- **대표 지적** 「불러오기가 전체적으로 다 느린데 이유가 뭐야?」 → 서버 A/B 실측으로 원인 확정 → PR #172 · Deploy #414.
  **원인 = 업체 표 안의 상세 HTML 덩어리**(업체당 1.1MB · 774MB). 그 뒤 칸(계약단계 등)을 읽는
  모든 목록 조회가 덩어리를 통과해 **300배** 느렸다(2ms vs 635ms).
  옆 표로 분리 + 압축(5배) · 수집 원문 14일 보관정책(지우는 규칙이 아예 없었다) · VACUUM 은 새벽에만.
  **실측 효과 = 업체 목록 13초 → 3ms · 덩어리 뒤 칸 635ms → 1ms · 남은 덩어리 0곳.**
  ⚠️ 파일 축소는 **오늘 03:10 VACUUM** 후 확인. ⚠️ 분석 집계(1,871ms)는 다음 후보로 남겼다.
  상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 배포 완료 (2026-08-30) — 「삭제 필요」 칸에서 바로 정리
- **대표 지시** 「삭제 필요에 있는 건 그 자리에서 삭제할 수 있으면 편리하지 않아?」 → 시안 확정 → PR #167 · Deploy #411.
  **지우지 않고 내린다**(`status='terminated'`) — 관리 목록·추적에서 빠지되 **기록은 보존**, 「🗄 내린 업체」 탭에서 ↩ 로 복귀.
  ⚠️ 하드 삭제를 붙이지 않은 이유 = 그 칸에는 **「전산에 없음」 오탐**이 섞인다(유성프레쉬→유성프레시 실사례).
  서버는 조회 1개만 가산, 저장은 기존 경로 재사용. 상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 배포 완료 (2026-08-29) — 세 건
- **순위 추적 화면 통합 + 키워드 양방향 「그만 재기」** — 대표 확정. PR #157 · Deploy #408.
  키워드를 **지우지 않고 억제**한다(`keyword_mute`) — 대표 키워드에서 빼도 분석 이력으로 되살아나던 구멍을
  한 표로 막았고, 업체·상품 **양쪽에서 함께** 빠진다. 재등록하면 이전 기록 그대로 복귀.
  업체 상세에 추적 상품 카드·출처 배지·✕ 버튼이 생겼고, 하단 도구는 **업체에 안 이어진 상품만** 보여준다.
  덤으로 **즉시 기록 경로의 자격 통일 누락**(계약 끝난 업체가 키워드만 겹치면 계속 기록되던 것)을 함께 고쳤다.
  8/30 서버 실측으로 표지·번들 전수 확인. 상세는 `docs/WORKLOG.md` 맨 위.
- **제안서 생성 기록을 스토어·플레이스로 갈라 본다** — 대표 지시. 컬럼은 이미 있었고 화면이
  `regular` 만 보내 둘이 뭉쳐 있었다. BE #570(deploy-vps #296) · FE #738(Cafe24 #1020).
  ⚠️ 소급 불가 — 구분 전 기록은 플레이스도 STORE 다. 화면이 「첫 PLACE 기록」 시각을 데이터에서
  뽑아 그보다 앞선 건수를 「구분 전」으로 밝힌다. 상세는 `docs/WORKLOG.md` 맨 위.
- **가게 이름 규칙이 브랜드스토어 주소도 읽는다** — 대표 지시. PR #159 · Deploy #409.
  호스트 접두어(`ss:`/`br:`)로 채널을 분리했다(겹침 0종 실측). 값은 저장되지 않아 소급 보정 불요.
  ⚠️ **지금 당장 이어지는 건 0건** — 브랜드 주소는 전체 3건이고 상품쪽·업체쪽 슬러그가 서로 다르다.
  앞으로 등록될 것을 위한 수정이다. 상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 해소 — 소상공인365 API 전면 503 (2026-08-30) · 경보 장치 배포 완료

> ⚠️ **아래 기록은 그대로 둔다**(경위가 필요하다). 결론만 먼저 적는다 —
> **API 는 12:24 경 스스로 복구**됐고(일시 장애), **키 만료가 아니었다**
> (같은 키로 다른 경로 = 업종 트리를 같은 시각에 부르니 **200·22KB 정상**).
> ⚠️ **교훈(2026-08-30 정정) — 「키가 유효한가」는 「인증이 실제로 필요한 경로가 값을 주는가」로만 답한다.**
> 처음엔 「같은 키의 다른 경로(업종 트리)가 200 이니 살아 있다」고 적었는데 **그 근거는 무효였다** —
> 그 경로는 진짜 키·틀린 키·키 없음이 전부 `200 · 22122B` 로 똑같다(인증을 아예 안 한다).
> 결론은 그대로지만 근거는 **상권 경로가 7곳에서 실제 매출값을 준 것**이다.
> 대표 선택 **「나」**(기록 + 매일 1회 자가 점검)를 **배포 완료**(PR #170 · Deploy #412).
> ⚠️ **배포 직후 실측으로 결함 1건을 잡아 함께 고쳤다**(PR #173 · Deploy #413) —
> 표본 첫 곳(성수동)이 **상시** 지역 판정에서 끊겨 **정상인 날에도 매일 실패가 쌓이던 것**.
> 표본을 실측 확인된 곳(구로동·역삼동·성사동)으로 교체하고 `region-unresolved` 를 기록에서 뺐다.
> ⚠️ **표본을 바꿀 때는 반드시 서버에서 돌려 「상권 API 까지 도달하는지」 확인할 것** —
> 도달하지 못하는 지역을 넣으면 그 경보는 만든 날부터 죽는다.
> ⭐ **경보를 만들 때는 「정상인 날에 무엇이 기록되는가」를 먼저 재 볼 것.**


- 11:50 · 11:55 KST 두 번 다 표본 3곳 전부 `HTTP Error 503`. 인증키 3종 보유 · 지역명→좌표 정상 ·
  좌표→행정동 정상 — **행정동→상권 데이터에서만** 죽는다. 8분 간격 동일이라 일시적 튐이 아니다.
- **영향** — `get_place_sbiz` 는 `main.py` 두 곳(플레이스 분석 · 제안서 enrich)에서
  **직원이 그 화면을 돌릴 때** 부른다(배치 아님). 지금 돌리면 상권 카드가 안 붙는다.
  서비스는 안 죽고 8/21 배포로 **생략 사유는 화면에 표시**된다.
- ⚠️ **언제부터인지 알 수 없다(실측 2026-08-30).** 컨테이너 기동(8/30 01:57 KST — 8/29 브랜드스토어
  배포)부터 지금까지 `[sbiz365]` 로그가 **한 줄도 없다**. 로그 0줄 = 성공도 실패도 없음 =
  **그 사이 아무도 플레이스 분석·제안서를 안 돌렸다**. 내가 본 503 은 전부 **진단이 직접 부른 것**이다.
  ⇒ 오늘 직원 피해는 **0건**. 다만 API 가 죽어 있으니 **다음에 돌리는 사람부터** 상권이 안 붙는다.
  ⇒ 시작 시점은 컨테이너 로그가 그만큼밖에 안 남아 확인 불가.
- **✅ 경보 장치 — 대표 「나로해」 확정 · 배포 완료 (PR #170 · Deploy #412)**(안은 셋이었다):
  **가** 기록만(호출마다 성공/실패·시각 남김 — 「언제부터」에 즉답) ·
  **나** 가 + **매일 1회 자가 점검**(아무도 안 써도 죽은 걸 안다) ·
  **다** 나 + 관리자 화면 배지(**시안 확정 필요**).
  ⇒ 추천 **나** — 화면 무변경이고, 직원은 이미 그 자리에서 사유를 본다(8/21 배포). **대표 확정 = 나.**
  **무엇이 돌고 있나** — `backend/sbiz_health.py` 신설(표준 라이브러리만). 호출마다 성공/실패·시각을
  `sbiz_health` 표에 남기고, **매일 05:00** 표본 3곳으로 자가 점검한다(아무도 안 써도 죽은 걸 안다).
  ⚠️ **시도조차 안 한 경우는 기록하지 않는다**(업종 미선택·지역 미확정·키 없음) —
     그것까지 실패로 세면 경보가 곧 쓸모없어진다.
  호출처(`main.py` 2곳)는 한 글자도 안 고쳤다 — 같은 이름의 wrapper 를 씌워 배선을 그대로 뒀다.
  ✅ **8/31 확인 완료** — 05:00 첫 실행 `🩺 소상공인365 자가 점검 — 정상 (표본 1개 중 확인)` ·
  `sbiz_health` 에 `출처=probe` 1건 생성 · `region-unresolved` 0건. **스스로 도는 것까지 실증됨.**
- ⚠️ **이것 때문에 3축(시간대·요일 유동인구 / SNS / 테마상권) 판정을 미룬다.**
  20개 경로가 전부 503 이라 「경로 미공개」로 읽었다가 취소했다 — 정상 경로도 같이 죽어 있어
  503 만으로는 「없다」와 「지금 안 된다」를 못 가른다. **API 회복 후 재측정.**
  ⚠️ **교훈: 「없다」를 증명하려면 「있는 것」이 지금 되는지를 같은 시각에 재야 한다.**
  ✅ **API 가 복구됐으니 이제 잴 수 있다.** 게다가 `sbiz_health` 가 그 시각 API 상태를 남기므로
     다음부터는 같은 오판을 반복하지 않는다(「503 이라 없다」인지 「그날 API 가 죽었다」인지 표가 답한다).

#### 📌 다음에 할 것
- **✅ 축A 즉시 기록 경로에 자격 판정이 없다 — 배포 완료 (PR #166 · Deploy #410, 2026-08-30)**
  ✅ **8/31 확인 완료** — 오늘 기록 113개 전부 정상 · **누수 0 · 미연결 0**(어제 15·10).
  8/30 에 시각으로 갈라 「15건은 오전 잔상」이라 예측한 것이 하루 뒤 그대로 맞았다.
  아래는 원인 규명 당시 기록(그대로 둔다).
  실측(2026-08-30): 오늘 기록된 추적 상품 196개 중 **자격 없는 업체에만 이어진 것 15개** ·
  **어느 업체에도 안 이어졌는데 기록된 것 9개**(연결 없는 41개는 전부 비활성인데도).
  ⚠️ **08:00 배치가 아니다** — 배치는 `eligible_tracked_product_ids()` 를 제대로 지난다.
  새는 곳은 `rank_record._tracked_targets()` 로, 조건이 **키워드 일치 하나뿐**이다.
  같은 파일 `_client_targets()`(축B)는 8/29 PR #157 로 고쳤는데 **축A 만 빠졌다.**
  ⚠️ 이 경로가 `check_type="scheduled"` 로 적어 **배치가 쓴 것처럼 보였다** — check_type 은 경로가 아니다.
  **고치는 법**: `_tracked_targets` 에 같은 판정 함수를 건다. DDL 0 · 신규 경로 0 · 화면 무변경 ·
  조회 실패 시 「전부 기록」 폴백 유지. ⚠️ **미연결 상품을 무조건 빼면 안 된다**(방금 등록분이 죽는다) —
  비활성 표시된 것만 뺀다.
- **소상공인365 미개방 3축 프로브** — 위 503 이 **풀렸다(12:24 복구)**. 재측정 가능. 열리는 것만 목록화해 시안으로.
  ⚠️ **「매출 축 하나만 쓴다」는 틀렸다(코드 실측)** — 이미 5축이 나간다. 남은 건 셋뿐.
- **✅ 진단 워크플로 스텝 멈춤 — 원인 확정·고침·실측 확인 (2026-08-30)**
  ⚠️ **내 첫 가설(끝의 `sudo pvs/vgs/lvs/dmidecode` 가 tty 없이 암호를 물어 멈춘다)은 틀렸다.**
  명령별로 timeout 15초를 씌워 재 보니 **전부 0~1초**였다(docker ps·inspect·logs · free · df ·
  lsblk · sudo 4종 · 컨테이너 안 DB 읽기까지).
  **진짜 원인** = 그 스텝 마지막의 「DB 테이블별 행 수」 블록이 **3.1GB DB 를 통째로 스캔**한다
  (전 테이블 `COUNT(*)` + `client_analyses`·`collected_serp` 의 JSON 컬럼 `SUM(LENGTH(...))`).
  DB 가 커지며 스텝 예산 240s 를 넘기기 시작했다 — 11:47 실행은 통과, 11:50 실행은 242초에 timeout.
  ⚠️ 그 스텝이 안 끝나면 **뒤 스텝이 전부 실행되지 않는다**(그날 진단이 세 번 막혔다).
  **고침**: 그 블록을 기본 끔으로(`dbsize=on` 일 때만). 실측 확인 — 문제 스텝 **20초·성공**,
  뒤 스텝 전부 정상, 런 전체 51초 success.
  ⚠️ 교훈 — **멈춤의 범인을 짐작하지 말고 명령마다 timeout 을 씌워 재라.** 짐작(sudo)이 틀렸다.
  📌 덤으로 본 것: DB **3.1GB** · 디스크 36G 중 21G(63%) · 백업 회당 585~589MB(gzip, 2개 보관).
     지금 조치할 일은 아니지만 커지는 속도가 붙는다.

#### 🌿 머지 대기 중인 진단 브랜치
- **`diag/stages`** — `stages`(계약 단계 반영) · `axis_a`(축A 누수 정밀 측정). 읽기 전용 · 업체명 미출력.
  ⚠️ 다른 세션이 `diag/verify-408` 에서 같은 `debug-rank.yml` 을 만지는 중이라 **일부러 미머지**.
  그쪽 머지 후 both-keep 으로 올린다.
  ⚠️ 만들며 실수 2건 — `docker exec` 에 **`-i` 누락**(출력 0줄인데 success 로 끝난다) ·
  서브쿼리 **여는 괄호를 파이썬 괄호로 착각**해 `IN SELECT ...` syntax error.

#### ✅ 확인해 보니 이미 끝나 있던 것 (목록에서 내림 — 2026-08-29)
> ⚠️ **이 세션에서만 세 건이 이미 끝나 있었다.** 배포해 놓고 목록을 안 지운 탓이다.
> 목록의 추정은 **코드·실측으로 먼저 확인할 것** — 「고치면 주인 없는 상품 40개가 준다」고 적었던
> 메모도 재 보니 브랜드 주소는 0개였다.
- **계약 단계 이름 받아 두기** → **배포됨**(8/28 PR #152 · Deploy #404). `clients.contract_stage` 저장 중.
- **2대 분할(큐도 같은 규칙으로)** → **배포됨**(8/27 PR #145 · Deploy #401). `/keywords`·`/requests` 양쪽 적용.
- **등록 화면에 업체 선택 칸(필수)** → **배포됨**(8/28 Deploy #403). 서버도 400 으로 최종 차단.
- **[서랍 대기] 수집 커버리지 — 깊이·폭·IP 3택** → 대표 확인 「다른 섹션에서 완료」.
  8/28 개편으로 하루 346 → 759(2.2배)이고 **2대 확장 불필요 확정**.
  ⚠️ 원인은 IP 도 깊이도 폭도 아니라 **확장 워커가 30초 무활동으로 죽던 것**이었다 —
  제가 좁혀 둔 3택 자체가 틀린 축이었다.

#### ✅ 「계속 열려 있는 것」도 네 줄이 이미 끝나 있었다 (목록에서 내림 — 2026-08-30)

> ⚠️ **바로 위 칸(대표 확인 대기)을 실측으로 고친 그 날, 이 칸은 그대로 두었다.**
> 대표 질문(「그럼 요청한거 다 된거지?」)에 「상태 미확인」이라고 답한 뒤 WORKLOG 을 실제로 뒤져 보니
> **네 줄 다 결론이 나 있었다.** 「※ 상태 미확인」이라고 적어 두는 것은 답이 아니다 —
> **그 자리에서 WORKLOG 을 찾아볼 것**(제목으로 검색하면 1분이면 나온다).

- **🔴 공식 경로 사실 확인** → **확인 완료**(WORKLOG 2026-08-11). 네이버 공식 쇼핑 검색 API 는
  **2026-07-31 종료**(404 SE05)이고, 오가닉 순위를 주는 다른 공식 API 는 없다
  (데이터랩=트렌드 지수 · 검색광고 API=광고 지표). **결론이 난 사항이지 열린 작업이 아니다 — 재조사 금지.**
- **⚠️ ①에 통지·요청 — 맞춤제안서 플레이스 프록시** → **통지 불요로 종결.**
  2026-08-12 에 **①이 우리에게 역으로 통지**해 왔다(「광고주 공유 대시보드가
  `POST /api/place/proposal-enrich` 를 하루 1회 추가 소비합니다」) = **①이 이미 알고 쓰고 있다.**
- **⚠️ 맞춤제안서(플레이스) 실데이터 화면 미표시 사고 — 원인 3건** → **배포됨**(2026-08-05 대표 「배포하자」).
- **맞춤제안서 후속 — 품목 11종 리뉴얼 + 업종 「종합」 집계** → **배포됨**(위와 같은 배포).

#### 📋 계속 열려 있는 것 (1건)
- 소상공인365 상권 데이터 **추가 고도화** — 기본 연동은 이미 배포됨(`sbiz365.py` 1,077줄 동작 중), 시안 v2 대기

#### ✅ 확인해 보니 이미 끝나 있던 것 (목록에서 내림 — 2026-08-27)
- 백업 폴더 무한 증식 수정 → **배포됨**(gzip 압축 + `BACKUP_KEEP=2`)
- `client_analyses` 보관정책 → **매일 01:00 잡으로 돌고 있음**(`_run_client_analyses_retention`)
- 플레이스 순위 무인 추적 → **배포됨**(01:40 정리 잡까지)
- 맞춤제안서(플레이스) 실데이터 연동 → **배포됨**(`POST /api/place/proposal-enrich`)
- 수집기 차단 대응 → 대표 확정 「확장 프로그램으로 해결하고 있잖아」 — 별건으로 두지 않는다
- IP 재차단 + 오판 사슬 3건(v1.6.0) → 확장이 이미 **1.7.0** 이라 포함됨
- 보고서 저장 시각 → 대표 확정 「코드까지 만질 필요 없다, 한국 시간 표기만」

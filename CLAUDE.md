# CLAUDE.md — 로직분석 작업 가이드 (필독)

> 새 세션은 작업 시작 전 이 파일을 먼저 읽는다.
> 이 저장소는 **로직분석(네이버 쇼핑 순위·SEO 분석)** 서비스다(`logic.metainc.co.kr`).
> 회사는 **여러 세션(섹션)이 동시에** 작업하므로 아래 섹션 경계와 동시작업 규칙을 지킨다.

## 섹션 분리 구조 (4섹션) — 운영자 지시 2026-07-11

> 전사를 4개 개발 섹션으로 **명확히 분리**해 개발·배포한다. 각 섹션은 자기 저장소만 손대고,
> 경계를 넘는 의존은 **코드 직접 수정이 아니라 '통지/계약'** 으로 다룬다.

| # | 섹션 | 소유 범위 | 우리(이 저장소) 관계 |
|---|---|---|---|
| ① | **메타 전산**(=메인 전산) | 전산/ERP + **BE 서버·무중단 배포·호스트/도메인**(`api.metainc.co.kr`) | 우리는 **소비자**(SSO·제안서 저장이 전산 호스트 의존) |
| ② | **로직분석 + 맞춤제안서** | **이 저장소** + FE `metainc-web-frontend/public/proposal/` | ★ **이 섹션 = 우리** |
| ③ | **광고센터 + 광고 생성툴** | `dashboard.metainc.co.kr` + 광고 스튜디오 | 다른 섹션 |
| ④ | **단독 메타 체험단** | 체험단(독립 섹션으로 분리) | 다른 섹션(우리 범위 밖) |

> ⚠️ **별도 '인프라 섹션'은 없다**(정정 #302, 2026-07-11). BE 서버·무중단 배포 파이프라인·호스트/도메인은 **① 메타 전산 소유**다. (과거 인계서의 "VPS 이전 섹션"은 별도 섹션이 아니라 ②의 당시 작업을 가리킴.)

> ### 🔴 ③ 광고센터와 **같은 물리 자원**을 쓴다 — 통지 대상 (2026-08-31 등재)
>
> 종전 통지 계약에는 **① 메타 전산만** 있었다. 8/30 전수조사에서 ②와 ③이
> **같은 서버 · 같은 사무실 IP · 같은 self-hosted 러너 · 같은 VPS 시크릿**을 공유한다는 것이
> ③ 저장소 문서로 확인됐다(③ 문서 원문: 「같은 서버의 **로직분석(②)**·스튜디오와 동거」).
>
> **이미 사고가 한 번 났다** — 스튜디오 세션 쿠키가 `/logic/` 로도 전송돼
> 「스튜디오 로그인 → **로직분석 로그인 화면으로 강제 이동**」(③ CHANGELOG v1.0.1).
> ③이 `/etc/nginx/sites-enabled/logic-analysis` 를 직접 편집한 기록도 있다.
>
> **우리가 건드릴 때 ③에 먼저 알린다**: `logic.metainc.co.kr` nginx 설정 · 포트(우리 5050) ·
> self-hosted 러너 구성 · 서버 디스크 정리.
> **③에 부탁한 것**: 스튜디오 5060 · autobid 5070 · nginx · 러너 · `docker system prune` 시 우리에게 통지.
>
> ⚠️ **`docker system prune -af` 주의** — ③ 문서가 공유 서버 대상으로 이걸 권한다.
>    그대로 돌면 **우리 롤백 이미지·빌드 캐시까지 지워진다**(우리는 blue-green 이라 롤백 이미지가 필요하다).
>
> 🟡 **자동입찰 — 대표 회신 수신(2026-08-31): 「후속 개발을 못 하고 있다. 지금은 개발 대기 상태」**
>    ③의 자동입찰 에이전트가 「**사무실 회선에서 10분마다**」 네이버 쇼핑을 크롤하는 코드가 있다
>    (`autobid/agent/autobid_agent.py`, 418/405 차단 대응·브라우저 위장 포함).
>    **우리 수집 확장이 도는 그 맥미니·그 IP 다.**
>    ⚠️ 우리는 8/28 캡차·IP 차단 원인을 「핫스팟 IP 평판」으로만 적어 뒀는데,
>       **같은 IP 를 두 섹션이 나눠 쓰는 것이라면 원인 축이 하나 더 있다.**
>       우리만 시간당 상한을 60→40 으로 조인 셈이 된다.
>
>    ⚠️⚠️ **「개발 대기」와 「가동 중지」는 다른 축이다 — 섞지 말 것.**
>       개발이 멈춰도 이미 띄워 둔 프로세스는 계속 돈다.
>
>    ### 🔴 실측 확정 (2026-08-31) — **autobid 는 4주째 가동 중이다**
>    공유 서버 `docker ps` 실측. 컨테이너 6개 중 **3개가 autobid** 다:
>    ```
>    NAMES              IMAGE                        STATUS            PORTS
>    autobid_engine     autobid-autobid-engine       Up 4 weeks
>    autobid_watchdog   autobid-autobid-watchdog     Up 4 weeks
>    autobid_web        autobid-autobid-web          Up 4 weeks        0.0.0.0:5070->5070/tcp
>    logic-analysis     logic-analysis:latest        Up About an hour  127.0.0.1:5050->5050/tcp
>    content-studio     content-studio:latest        Up 7 weeks        127.0.0.1:5060->5060/tcp
>    ad-api             ad-dashboard-deploy-ad-api   Up 51 minutes     127.0.0.1:5051->5051/tcp
>    ```
>    ⇒ **「개발 대기」는 맞지만 「안 돈다」는 아니다.** 4주 = 8/28 캡차 사고 기간을 통째로 덮는다.
>    ⇒ `autobid_engine` 은 서버에서 `msearch.shopping.naver.com` 을 직접 부른다(③ `ranker.py`).
>
>    ### 🔴🔴 확정 (2026-08-31 14:04 KST) — **에이전트도 지금 돌고 있고, 우리와 같은 회선이다**
>    에이전트는 `logic.metainc.co.kr/autobid` 로 보고하므로 **우리 nginx 를 지난다.**
>    그 접속 기록을 대조했다(⚠️ 저장소 공개라 IP 원문은 안 찍고 일치 여부·건수만):
>    ```
>    /autobid 요청 480건 · 보낸 곳 2개
>    /api/collector 요청 2714건 · 보낸 곳 4개
>    ★ 같은 곳에서 둘 다 보낸 경우: 1개
>      → 그 곳이 보낸 양: 자동입찰 458건 · 우리 수집 2611건
>    [현재 로그 기준] 마지막 /autobid 14:03:37 · 마지막 /api/collector 14:04:27
>                     오늘 건수 — 자동입찰 192건 · 우리 수집 945건
>    ```
>    ⇒ **같은 기계·같은 회선에서 둘 다 나간다.** 조회 시각 기준 **50초 전에도 자동입찰이 보고**했다.
>    ⇒ 「개발 대기」는 맞지만 **가동은 멈춘 적이 없다**(대표 회신 = 개발 상태였다).
>
>    ### ⚠️ 그래서 우리 8/28 캡차 진단을 고쳐 적는다
>    종전 기록: 「원인은 데이터 용량이 아니라 **IP 평판**이다 … 핫스팟은 여러 사용자가 돌려쓴다」.
>    **그 문장은 절반만 맞다.** 같은 회선에서 자동입찰이 하루 수백 건을 함께 내보내고 있었다.
>    우리는 그 몫을 모른 채 **우리 상한만 60→40 으로 조였다.**
>    ⇒ 수집 속도를 다시 정할 때 **③ 몫을 포함한 회선 총량**으로 계산해야 한다.
>    ⚠️ 다만 「자동입찰이 캡차의 원인이다」로 단정하지는 않는다 — **원인 축이 하나 더 있음이
>       확인된 것**이고, 어느 쪽이 얼마나 기여했는지는 아직 안 쟀다.
>
>    ### ✅ 수집 속도 — 대표 확정 (2026-08-31): **B안 = 지금 상한(40) 유지**
>    선택지는 둘이었다 — **A** 자동입찰 몫을 며칠 실측해 상한 재계산(정확하나 며칠 걸림) /
>    **B** 지금 40 유지하고 캡차가 재발하면 그때 대응(안전하나 수집량이 눌린 채로 간다).
>    **대표 확정 = B.**
>    ⇒ **코드 변경 없음.** `HOURLY_CAP = 40` 그대로 두고, 캡차 후 자동 감속도 그대로 둔다.
>    ⚠️ **다만 「눌린 채로 간다」는 것을 알고 가는 것이다** — 유니버스 814개인데 1대가
>       하루 759~1088개를 소화하니 지금은 여유가 있다. 수집량이 700 밑으로 떨어지면
>       그때가 A안(회선 총량 실측)을 꺼낼 시점이다.
>    ⚠️ **캡차가 재발하면 제일 먼저 볼 것** — 우리 상한이 아니라 **자동입찰 몫**이다.
>       이번에 그 축을 몰라서 우리 상한만 조였다. 같은 실수를 반복하지 말 것.
>
>    ⚠️ **교훈 — 사람의 답과 필요한 답이 다른 축일 수 있다.**
>       「개발 대기」를 「안 돈다」로 읽었으면 원인을 영영 못 찾았다. 축이 다르면 직접 잰다.
>    ⚠️ **교훈 — 로그를 이어 붙일 때 순서가 곧 시간축이다.**
>       첫 판에서 `[현재, 회전분]` 순으로 붙이고 마지막 줄을 읽어 **회전 경계(00:00)를
>       「마지막 요청 시각」으로 오독**할 뻔했다. 시각 판정은 현재 로그만 본다.
>    ⚠️ **`autobid_web` 만 `0.0.0.0:5070` 로 열려 있다** — 나머지 셋(우리 5050·스튜디오 5060·
>       광고센터 5051)은 전부 `127.0.0.1` 바인딩이다. ③ 판단 사항이나 통지문에 적어 둘 것.
>
>    ⚠️ **교훈 — 「대표가 개발 안 한다고 했으니 안 돈다」로 넘어갈 뻔했다.**
>       사람의 답은 「개발 상태」였고 우리가 필요한 건 「가동 상태」였다. 축이 다르면 다시 잰다.
>    ⚠️ **진단 함정** — `hang_probe` 의 `probe()` 는 출력을 `/dev/null` 로 버리고 소요 시간만 잰다.
>       `docker ps` 를 probe 로 돌려도 목록이 안 남는다. 목록은 따로 찍어야 한다(그래서 한 줄 추가했다).
>    통지문: ③ 저장소 `docs/로직분석-통지-2026-08-31.md` (PR metainc-ad-dashboard#1)
>
> ✅ **데이터 계약은 문제 없다**(8/30 전수 확인) — ③ 신규 설계(A안)는 우리 API 미사용 ·
>    ③ 수집 스케줄러 06:00 KST 로 우리 04:00 과 분리 유지 · ③ 저장소에 우리 필드 참조 0건.

**경계 원칙**
- **우리 코드만 수정한다.** 전산·광고센터·체험단 저장소의 코드·거버넌스는 건드리지 않는다.
- 전산 호스트(도메인·서버) 변경 등 **경계 너머 의존이 깨질 위험**은 직접 고치지 말고, 호스트 소유 섹션(**① 메타 전산** — BE 서버·호스트·배포 소유)에 **사전 통지·계약**으로 처리한다.
  - 우리가 전산 호스트에 의존하는 지점: `backend/auth.py`(`ERP_BASE_URL` — SSO 검증), FE `public/proposal/index.html`(`API_BASE` — 제안서 저장/조회), `backend/scheduler.py`(`/api/ad-sync/contracts` — 계약단계 동기화 **04:00**(구 07:30, 2026-07-27 변경), `ERP_AD_SYNC_API_KEY`).
  - **✅ ad-sync 소비 합의(2026-07-20).** ① 소유 `GET /api/ad-sync/contracts`를 ②도 소비(계약 만료·환불중·홀딩중 업체 자동 추적 자동 중지 / 진행·전략 관리·사후 관리 복귀 시 재개). 하루 1회 **04:00**(구 07:30 — 2026-07-27 업무시간 502 사고로 전 배치를 새벽 이동, ③의 06:00과 분리 조건은 그대로 유지)·읽기 전용·단계당 1콜. ⚠️ **호출 시각 변경은 ①에 통지 대상**(합의문에 07:30으로 기재돼 있었음). **단계명 문자열이 곧 계약**(ERP_CONTRACT_SYNC_API.md) — ① 단계명 변경 시 ②에도 사전 통지 대상. 키는 서버 `.env` `ERP_AD_SYNC_API_KEY`(미설정 시 무동작). 직원 수동 토글(`auto_analysis_manual`)이 항상 우선.
  - **✅ portal-summary 검색량 연동 활성(2026-08-04).** ①이 형식 필드 대조 후 **스위치 ON·무중단 배포 완료**(`portal.seo-auto-enabled=true`, env `PORTAL_SEO_AUTO_ENABLED=false`로 재정의 가능). **① 파서 = `ClientPortalService.parseLogicSummary()` → `VolumeRow(keyword·volume·rank)`** — `seo.keywordVolume[]` 3필드명·구조는 **계약 고정, 변경 시 사전 통지**. ① 소비 방식: 매일 **04:40 KST** 배치가 P10 계약 품목 보유 업체 대상 portal-summary 원문을 `mp_client_logic_cache`에 캐시(즉시 반영은 ① 「⟳ 지금 갱신」). ✅ **시각 정합 완료(2026-08-04, A안 채택·대표 확정)**: ① 캐시 배치 **04:40 → 08:40 이동**(② 08:00 순위 기록 후 소비 = 당일 순위 노출, ① 커넥션 분리 안전장치 동반). ⚠️ **상호 계약**: ②가 08:00 순위 기록 시각을 08:35 이후로 늦추려면 ①에 사전 통지(①의 08:40 캐시가 전일값으로 회귀함). 단기 유의: 8/4 당일은 구스케줄 배치 커버리지 76%라 일부 키워드 rank 가 「-」(8/1~3 결측 잔상) — 8/5 전량 커버부터 완전 정상.
  - **✅ portal-summary 검색량 제공 합의(2026-07-28).** ①이 소비하는 **`GET /api/cd/portal-summary`** 를 계약 목록에 등재(종전 누락). ① 요청으로 응답에 **`seo.keywordVolume[]`**(`keyword`·`volume`·`rank`) 추가 — 값 출처는 우리가 이미 가진 데이터(검색량=`client_analyses`→`summaryCards.totalVolume` / 순위=`client_rank_history` 키워드별 최신 `rank_position`)라 **신규 크롤링·API 호출 없음**. 제약 3가지를 ①에 고지: **최대 8개 키워드**(`LIMIT 8`) · **자동 분석 이력이 있는 업체만** · **300위 밖은 `"-"`**(04:30 추적 범위). 검색량 있는 키워드가 하나도 없으면 **`seo` 키 자체를 생략**(① 담당자 입력값으로 폴백). ⚠️ **기존 4필드(`found`·`clientId`·`insight`·`dailyReports`·`totalDays`) 의미·형식 변경은 ①에 통지 대상.**
  - **✅ 호출 다이어트 통지 합의(2026-07-20).** ②의 검색 API 다이어트(D1~D5, PR #91·#92)를 ①이 검토 — `LogicAnalysisClient` 소비 4경로 스키마 영향 없음·이의 없음 확인(① 작업 브랜치 커밋 15ecff9). 소진 재발 시 ② 90% 경보를 ①에 공유하기로.
  - 2026-07 전산 BE 호스트 이전(`metainc01.cafe24.com` → `api.metainc.co.kr`)이 통지 없이 진행돼 조용한 중단 위험이 있었음 → SSO는 신 호스트로 갱신, 제안서 폴백도 신 호스트로 교체(FE PR #301, 2026-07-11 배포 완료).
  - **✅ 통지 계약 성립(2026-07-11).** ① 메타 전산이 회신으로 **호스트/도메인/서버 변경 시 ③ 광고센터 + ② 로직분석에 사전 통지**를 확약하고, 전산 CLAUDE.md에 반영(**`my-profile` 소비자에 ② 등재**). 옛 호스트는 **전환 유예(리다이렉트/병행) 후** 내림. 현재 예정된 호스트/도메인 변경 계획 없음. → 다음 호스트 변경은 사전 통지로 우리가 무중단 대응.

## 배포 규칙

- 배포는 `main` 푸시 → GitHub Actions가 VPS로 SSH/SCP + `docker compose` 재기동(회귀 게이트 → 빌드 후 스왑 → 헬스체크 → 실패 시 자동 롤백). **정적 FE와 달리 실서비스**이므로, 배포는 운영자 지시("배포하자") 시에만.
- 업무시간 중 임의 `main` 직푸시 금지 — 미승인 작업은 feature 브랜치에 stack(미머지).
- 커밋 메시지 · 코드 · PR 본문에 **AI/모델 식별자 금지**. 한글은 UTF-8.
  - ⚠️ **이 규칙은 저절로 지켜지지 않는다 — 두 곳이 자동으로 붙인다**(2026-08-27 확인·수정).
    ① **커밋 작성자** — 이 작업 환경의 git 기본값이 `Claude <noreply@anthropic.com>` 이라,
       스쿼시 머지 때 main 커밋에 `Co-authored-by:` 로 박힌다. **착수할 때 저장소 설정을 먼저 바꿀 것**:
       `git config user.name "metainchelp-boop" && git config user.email "metainchelp@gmail.com"`
    ② **PR 본문** — 생성 도구가 본문 끝에 서명 한 줄을 붙인다. **PR 을 연 뒤 본문을 다시 확인해 지울 것.**
  - ⚠️ 이미 머지된 이력은 되돌리지 않는다(공유 히스토리 재작성이 더 위험하다). **머지 전에만 고친다.**

## 동시작업 충돌 방지 (항상)

1. **편집 전 동기화** — 공유 파일 수정 전 `git fetch origin` 후 최신 `main` 확인, 필요 시 rebase.
2. **다른 섹션/작업 확인** — 시작·머지·배포 각 시점에 열린 브랜치/PR 확인(`mcp__github__list_branches`, `list_pull_requests`). 같은 파일을 만지는 동시 작업이 있으면 영역 분리·순서 조정으로 충돌 회피.
3. **작업 브랜치 stack** — 각 작업은 지정 feature 브랜치에서 진행.
4. **머지/배포 직전 재확인** — 머지 직전 다시 fetch + 충돌 점검 + 회귀 테스트/빌드 검증.
5. **연속성 메모리(append-only)** — 진행 상태를 문서에 누적 기록해 다음 세션이 이어받게 한다. 기존 항목 삭제·요약 금지.

## 산출물 전달 형식 (항상 — HTML, 크롬에서 바로 열람)

- 문서·리포트·체크리스트·목업·요약 등 산출물은 **자체완결 HTML 파일**(인라인 CSS, 외부 의존성·네트워크 없이 단일 파일)로 만들어 `SendUserFile`로 전달한다. 전달본 파일명·`<title>`은 **한글**로.
- 전산 톤: 표준 파랑 `#3b82f6`, 슬레이트 회색, 한글 UTF-8.

## 현재 진행 중 작업 (append-only)

- **[배포 대기 2026-09-03] 신고 #259 (이진희) — 업체 삭제 시 「FOREIGN KEY constraint failed」 [BE·삭제 순서 1줄]** — 전산(①)에서 이관받아 여기서 처리(대표 「B로 여기서 하자」). `client_dashboard.delete_client` 이 clients 를 지우기 전 `reports`(cascade 없는 유일한 자식)를 안 지워 보고서 남은 업체 삭제가 FK 오류로 막히던 것. 삭제 루프에 `DELETE FROM reports` 1줄 추가. 회귀 테스트 `backend/tests/test_client_delete_reports.py` 4/4 + 게이트 스텝. 브랜치 `fix/client-delete-reports-fk-259`. 대표 「배포하자」 후 main 머지. 상세는 `docs/WORKLOG.md` 맨 위.
- **[진행 중 2026-08-28] 담당자 변경 전체 이관 API·과거 보고서 운영권한 연계 [로직분석]** — 상세는 `docs/WORKLOG.md` 맨 위.

> ⚠️ **완료된 차수의 상세 기록은 [`docs/WORKLOG.md`](docs/WORKLOG.md) 로 옮겼습니다** (2026-08-21 대표 확정).
> **한 줄도 지우지 않았습니다** — 「기존 항목 삭제·요약 금지」 규칙은 그대로이고, 쌓이는 자리만 옮긴 것입니다.
> 이 파일이 44만 토큰까지 불어나 세션이 시작조차 못 하던 것을 해소하기 위함입니다(규칙 4% · 이력 96%).
>
> **지난 차수의 경위·교훈·배포 기록을 찾을 때** — `docs/WORKLOG.md` 에서 업체명·기능명·날짜로 검색하세요.
> 아래 목록의 제목을 그대로 복사해 검색하면 그 항목 원문이 나옵니다.
>
> **새 차수를 적을 때** — 진행 중에는 아래에 한 줄로 적고, 상세는 `docs/WORKLOG.md` 맨 위에 적습니다.
> **배포가 끝나면 아래 한 줄을 지웁니다**(상세는 WORKLOG 에 그대로 남습니다). 이 규칙이 이 파일을 다시 커지지 않게 합니다.

### 🔓 아직 열려 있는 작업 (13건) — 2026-08-27 전수 확인

> ⚠️ **이 목록을 12건에서 다시 세었다.** 대표 지시(「이미 확정되서 처리해야 하는 리스트면 바로 처리해」)로
> 12건을 하나씩 **코드로 확인**했더니 **6건이 이미 서버에서 돌고 있었다** — 배포해 놓고 목록만 안 지운 것이다.
> 앞으로는 배포 확인과 같은 자리에서 이 목록을 정리한다(그 손이 빠져서 넉 달치가 쌓였다).

#### ✅ 오늘 배포 완료 (2026-08-27)
- **여러 대로 나눠 돌리기** (#145) — 대표 「1번 기기 앞쪽, 2번 기기 뒤쪽」.
  등록 순번이 아니라 **키워드 글자로** 가른다(중간이 지워져도 경계가 안 흔들린다).
  실측 871개 기준 2대 437/434 · 겹침 0 · 누락 0 · 치우침 0.3%.
  ⚠️ **큐를 같이 나누는 게 핵심** — 순위 목록만 나누면 두 대가 5회 재시도 한도를 2.5회에 태운다.
  `split_rule.py` 신설(배포 게이트 환경에 fastapi 가 없어 collector.py 는 검사 불가라 규칙을 따로 뺐다).
  확장 **v1.8.0** — 「n번 기계 / 전체 m대」 + 토큰 보기(👁). 서버 파일 실측 확인(collector 32,480 · split_rule 2,149).

#### ✅ 8/28 확인 결과 — 어젯밤 예정분
1. **01:20 자동 연결 — 예측대로 정확히 86건.** `백필 적용: +86건 (총 390)` · 고아 정리 1건.
   주인 없는 상품 **126 → 40개**.
2. **08:00 나눠 적기 — 배선 확인**(`이어진 짝 347건`). ⚠️ 완료 줄(`나눠 적음 n건`)은 **아직 미확인** —
   그 시각 배치가 진행 중이었다(수집분 없는 키워드마다 18초 대기라 오래 걸린다). **저녁에 재확인할 것.**
3. **2대 분할 — 핫스팟 IP 로는 실패.** ⚠️ 아래 참조.

#### 🛑 핫스팟 IP 로 2대 확장은 안 된다 (2026-08-28 실측)
대표가 맥북을 **핸드폰 핫스팟**에 붙여 시도 → **돌리자마자 캡차**, 캡차를 풀고 재실행해도 **첫 요청에서 다시 캡차**.
서버 기록으로도 맥북 업로드 **0건**(수집 간격이 40~50초로 한 대 속도 그대로 = 맥미니만 일함).

⚠️ **원인은 데이터 용량이 아니라 IP 평판이다.** 통신사 핫스팟은 여러 사용자가 돌려쓰는 대역이라,
   새 IP 처럼 보여도 이미 네이버에 찍힌 상태를 물려받는다. 캡차를 풀어도 IP 기반 차단이라 계속 걸린다.
⚠️ **반복 실행 금지** — 누를 때마다 차단이 깊어진다(확장이 6시간 쉬게 해 둔 이유).
⇒ **2대 확장은 집 유선 IP 로 다시 시도한다.** 핫스팟 안은 폐기.

⚠️ 제가 「IP 는 확실히 갈립니다」라고 답한 것이 짧았다 — 갈리는 것과 **쓸 수 있는 IP 인지**는 다른 문제였다.

#### 🔥 대표 확인 대기 — 2026-08-30 실측으로 전면 재작성

> ⚠️ **이 칸이 또 낡아 있었다.** 대표 「2,3,4 다 끝난거 아냐?」 지적으로 서버 실데이터를 다시 셌더니
> 네 줄 중 **두 줄은 이미 해소**, 한 줄은 **대표 몫이 아니라 내 몫**이었다.
> **「대표 확인 대기」에 넣기 전에 그것이 정말 사람이 결정할 일인지 먼저 물을 것** — 내가 할 수 있는
> 일을 여기 넣어 두면 아무도 손대지 않은 채로 몇 주가 지난다.

- **추적 기간 지난 업체 78곳** — 직원 확인표 https://claude.ai/code/artifact/19fb6b57-447c-43a7-a4d1-47d364c81406
  ⚠️ **급하지 않다(실측 2026-08-30).** 「낭비 수집을 멈춘다」는 목적은 **이미 달성됐다** —
  오늘 순위가 기록된 업체 333곳 중 **자격 없는 곳 0곳**(8/29 PR #157 이 즉시 기록 경로까지
  `eligible_clients_sql` 로 통일한 효과). 남은 건 화면에 「기간 지남」으로 남아 있는 명단 정리뿐이다.
  ⚠️ 수는 76 → **78곳**으로 늘었다(계약 종료가 계속 들어온다). 14일 안에 끝나는 곳이 15곳 더 있어
  **명단을 한 번 훑는 방식으로는 영원히 안 끝난다** — 전산에서 단계를 옮기면 다음 날 04:00 이 따라간다.
- **✅ 주인 없는 추적 상품 — 「40개」는 낡은 수였다.** 8/29 실측 `disabled_at` 기준 **0개**.
  단 축A(`tracked_products`)의 **미연결 41개**는 그대로다 — 이 둘은 다른 축이다
  (「휴면 처리됐나」 vs 「업체에 이어졌나」). 아래 새 항목 참조.
- **✅ 2대 확장 — 불필요 확정(실측 2026-08-30).** 유니버스 **814개**인데 실제 소화가
  8/28 **759** · 8/29 **1088** 로 **1대가 이미 전량을 넘겼다**(개편 전은 344~359).
  시간대 몫도 어느 시간대든 서버 상한 40 아래이고 **50분 예산 중 11분만 쓴다** — 여유가 많다.
  ⚠️ 진단 ⑥ 표가 「2대 필요」로 나오는 건 1대 능력을 **10일 평균 475개**로 잡기 때문이고,
  그 평균에 개편 전 저성능 8일이 섞여 있다. **평균이 아니라 개편 후 실측으로 판단할 것.**
- **✅ 소상공인365 — 「매출 축 하나만 쓴다」는 틀렸다(코드 실측).** 이미 5축이 나간다:
  `sales`(점포당 월매출·최소·최대·구/시 벤치마크) · `salesSeries`(월별 추이) · `shops`(업소수+시계열+증감) ·
  `traffic.dayAvg`(유동인구) · `topFive`(업종 상위 5개 행정동) · `major`(대분류 전체).
  아직 비어 있는 축은 **셋뿐** — `traffic.hours[6]`·`days[7]`·`weekendShare`(시간대·요일) · `sns` · `theme`.
  ⇒ **대표 확인 대기가 아니라 내 몫이다**(그 셋이 실응답에서 열리는지 읽기 전용 프로브로 재 보는 일).
  아래 「다음에 할 것」으로 옮긴다.

#### ✅ 머지 완료 (2026-08-28 · PR #155) — 플레이스 추적기 팝업 개편 v1.1.0
- 대표 지시 「디자인이 쇼핑 순위 추적하고 동일하게 · 노출되는 정보도 쇼핑 쪽에 맞춰서」.
  보라 → 전산 파랑 통일 · 오늘 수집 진척 막대 · 노출/미노출/미확인 색 구분 · 진행 중 키워드 ·
  로그 창(`place_logs` — 쇼핑과 같은 방식). **수집 동작은 무변경**(로그·상태 조회만 가산).
  `extension-place/**` 는 paths-ignore 라 **서버 재배포 없음** — 적용은 맥미니 폴더 교체(zip 전달함).
  시안 https://claude.ai/code/artifact/b4320fd1-a756-4b6b-93ac-a0f79d614a77

#### ✅ 배포 완료 (2026-08-28 · Deploy #403·#404) — 화면 두 건
- **순위 추적 등록칸을 맨 위로 + 업체 칸 신설(필수)** — 등록까지 세 단계였고 업체를 적을 칸이
  아예 없어 **주인 없는 상품 41개**가 생겼다. 서버는 8/27 에 받을 자리를 만들어 뒀는데 화면 짝이
  없었다. 이제 업체는 **목록에서 고른 것만** 저장되고 서버가 400 으로 최종 차단.
  ⚠️ **절차 사고** — 승인 없이 `main` 푸시로 배포가 나갔다(#403). 다음부터 `.md` 아닌 파일은
  승인 전까지 브랜치에만 둔다.
- **업체 목록 5칸 분리**(진행중·환불중·홀딩중·삭제 필요·확인 필요, PR #152) — 04:00 동기화가
  전산 계약 단계를 읽고 **이름을 버리던 것**을 저장하게 했다(전산 작업 0).
  삭제 필요는 사유 배지로, 확인 필요는 **수집 5회 소진**만(순위 없음 260 중 251 이 정상 「300위 밖」).
  ⚠️ **~~대표 조치 필요~~ → ✅ 2026-08-30 대표가 눌렀고 실측으로 반영 확인.**
  활성 703곳 중 **614곳(87%)** 에 단계가 채워졌다 — 진행중 478 · 사후 관리 25 · **환불중 18** ·
  **홀딩중 30** · 계약 만료 58 · 전산 외 5 · (없음) 89.
  **자동분석 OFF 126곳이 전부 단계를 갖고 있다**(126/126) = 04:00 동기화 정상.
  ⚠️ **버튼 위치를 `#management` 로 적어 둔 것은 틀렸다** — 실제로는 **`#home`(🏠 대시보드)**
  의 「🏢 등록 업체」 구역, 칸 나누기 알약 줄 **맨 오른쪽**이다(`ClientListSection.jsx`).
  `#management` 는 `ClientDashboard` 라 그 버튼이 없다. 대표가 못 찾아 두 번 되물으셨다 —
  **화면 경로를 적을 때는 그 컴포넌트가 어느 `currentPage` 에서 렌더되는지 코드로 확인할 것.**
  상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 배포 완료 (2026-08-28 · PR #151 · Deploy #402)
- **수집이 시간당 2~3개로 굶던 원인 4건** — 상세는 `docs/WORKLOG.md` 맨 위.
  ⭐ **진짜 원인 = 확장이 30초마다 잠들었다**(MV3 워커는 30초 무활동이면 종료. 키워드 사이
  휴식이 30초라 회차가 첫 키워드에서 끊겼다 — 온디맨드는 20초라 살아남아 상한 12건을 다 채웠다).
  하루 345개 중 288개가 밀린 큐 몫이고 순위 추적은 57개뿐이었다(재야 할 양 864개).
  함께 고침: 큐 되먹임 차단 · 자격 판정 3곳 통일(계약 끝난 65곳 기록 정지) · 주인 없는 상품 41개 정리 ·
  시간당 상한 60→40 · 캡차 후 하루 절반 속도 자동 감속. 확장 **v1.9.0**.
  **서버 배포 확인**(진단 #64) = 파일 전부 12:46 타임스탬프 · `tracking_eligibility.py` 4,496B 신규 ·
  `HOURLY_CAP = 40` · `prune_self_tail_requests` · `eligible_clients_sql` 3곳 · `enqueue_on_miss=False` 6곳 ·
  `disable_ownerless` — 표지 전부 확인. **부팅 +2분 큐 정리 실행됨: `329건 제거 · 남은 대기 476건`**
  (밀린 큐 813 → 476). 유니버스 864개 · 자격 업체 465곳 · 오류 0.
  ⚠️ **맥미니 확장 v1.9.0 교체는 대표 진행 중** — 서버만으로는 원인 ②③④만 적용되고
  가장 큰 몫인 ①(회차 끊김)과 감속은 확장 쪽이다.
  ⚠️ **2대 확장은 이걸 고치고 사흘 재본 뒤 판단** — 대수 문제가 아니었다.
  📌 **다음 확인** = 하루 뒤 `collected_serp` 일일 건수(기준 345개) · 01:20 `주인 없어 추적 중지 n건` ·
  08:00 `추적 상품 자격 판정 — n개 중 m개만 잰다`.

#### ✅ 배포 완료 (2026-08-30) — 전체 느림 해소
- **대표 지적** 「불러오기가 전체적으로 다 느린데 이유가 뭐야?」 → 서버 A/B 실측으로 원인 확정 → PR #172 · Deploy #414.
  **원인 = 업체 표 안의 상세 HTML 덩어리**(업체당 1.1MB · 774MB). 그 뒤 칸(계약단계 등)을 읽는
  모든 목록 조회가 덩어리를 통과해 **300배** 느렸다(2ms vs 635ms).
  옆 표로 분리 + 압축(5배) · 수집 원문 14일 보관정책(지우는 규칙이 아예 없었다) · VACUUM 은 새벽에만.
  **실측 효과 = 업체 목록 13초 → 3ms · 덩어리 뒤 칸 635ms → 1ms · 남은 덩어리 0곳.**
  ⚠️ 파일 축소는 **오늘 03:10 VACUUM** 후 확인. ⚠️ 분석 집계(1,871ms)는 다음 후보로 남겼다.
  상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 배포 완료 (2026-08-30) — 「삭제 필요」 칸에서 바로 정리
- **대표 지시** 「삭제 필요에 있는 건 그 자리에서 삭제할 수 있으면 편리하지 않아?」 → 시안 확정 → PR #167 · Deploy #411.
  **지우지 않고 내린다**(`status='terminated'`) — 관리 목록·추적에서 빠지되 **기록은 보존**, 「🗄 내린 업체」 탭에서 ↩ 로 복귀.
  ⚠️ 하드 삭제를 붙이지 않은 이유 = 그 칸에는 **「전산에 없음」 오탐**이 섞인다(유성프레쉬→유성프레시 실사례).
  서버는 조회 1개만 가산, 저장은 기존 경로 재사용. 상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 배포 완료 (2026-08-29) — 세 건
- **순위 추적 화면 통합 + 키워드 양방향 「그만 재기」** — 대표 확정. PR #157 · Deploy #408.
  키워드를 **지우지 않고 억제**한다(`keyword_mute`) — 대표 키워드에서 빼도 분석 이력으로 되살아나던 구멍을
  한 표로 막았고, 업체·상품 **양쪽에서 함께** 빠진다. 재등록하면 이전 기록 그대로 복귀.
  업체 상세에 추적 상품 카드·출처 배지·✕ 버튼이 생겼고, 하단 도구는 **업체에 안 이어진 상품만** 보여준다.
  덤으로 **즉시 기록 경로의 자격 통일 누락**(계약 끝난 업체가 키워드만 겹치면 계속 기록되던 것)을 함께 고쳤다.
  8/30 서버 실측으로 표지·번들 전수 확인. 상세는 `docs/WORKLOG.md` 맨 위.
- **제안서 생성 기록을 스토어·플레이스로 갈라 본다** — 대표 지시. 컬럼은 이미 있었고 화면이
  `regular` 만 보내 둘이 뭉쳐 있었다. BE #570(deploy-vps #296) · FE #738(Cafe24 #1020).
  ⚠️ 소급 불가 — 구분 전 기록은 플레이스도 STORE 다. 화면이 「첫 PLACE 기록」 시각을 데이터에서
  뽑아 그보다 앞선 건수를 「구분 전」으로 밝힌다. 상세는 `docs/WORKLOG.md` 맨 위.
- **가게 이름 규칙이 브랜드스토어 주소도 읽는다** — 대표 지시. PR #159 · Deploy #409.
  호스트 접두어(`ss:`/`br:`)로 채널을 분리했다(겹침 0종 실측). 값은 저장되지 않아 소급 보정 불요.
  ⚠️ **지금 당장 이어지는 건 0건** — 브랜드 주소는 전체 3건이고 상품쪽·업체쪽 슬러그가 서로 다르다.
  앞으로 등록될 것을 위한 수정이다. 상세는 `docs/WORKLOG.md` 맨 위.

#### ✅ 해소 — 소상공인365 API 전면 503 (2026-08-30) · 경보 장치 배포 완료

> ⚠️ **아래 기록은 그대로 둔다**(경위가 필요하다). 결론만 먼저 적는다 —
> **API 는 12:24 경 스스로 복구**됐고(일시 장애), **키 만료가 아니었다**
> (같은 키로 다른 경로 = 업종 트리를 같은 시각에 부르니 **200·22KB 정상**).
> ⚠️ **교훈(2026-08-30 정정) — 「키가 유효한가」는 「인증이 실제로 필요한 경로가 값을 주는가」로만 답한다.**
> 처음엔 「같은 키의 다른 경로(업종 트리)가 200 이니 살아 있다」고 적었는데 **그 근거는 무효였다** —
> 그 경로는 진짜 키·틀린 키·키 없음이 전부 `200 · 22122B` 로 똑같다(인증을 아예 안 한다).
> 결론은 그대로지만 근거는 **상권 경로가 7곳에서 실제 매출값을 준 것**이다.
> 대표 선택 **「나」**(기록 + 매일 1회 자가 점검)를 **배포 완료**(PR #170 · Deploy #412).
> ⚠️ **배포 직후 실측으로 결함 1건을 잡아 함께 고쳤다**(PR #173 · Deploy #413) —
> 표본 첫 곳(성수동)이 **상시** 지역 판정에서 끊겨 **정상인 날에도 매일 실패가 쌓이던 것**.
> 표본을 실측 확인된 곳(구로동·역삼동·성사동)으로 교체하고 `region-unresolved` 를 기록에서 뺐다.
> ⚠️ **표본을 바꿀 때는 반드시 서버에서 돌려 「상권 API 까지 도달하는지」 확인할 것** —
> 도달하지 못하는 지역을 넣으면 그 경보는 만든 날부터 죽는다.
> ⭐ **경보를 만들 때는 「정상인 날에 무엇이 기록되는가」를 먼저 재 볼 것.**


- 11:50 · 11:55 KST 두 번 다 표본 3곳 전부 `HTTP Error 503`. 인증키 3종 보유 · 지역명→좌표 정상 ·
  좌표→행정동 정상 — **행정동→상권 데이터에서만** 죽는다. 8분 간격 동일이라 일시적 튐이 아니다.
- **영향** — `get_place_sbiz` 는 `main.py` 두 곳(플레이스 분석 · 제안서 enrich)에서
  **직원이 그 화면을 돌릴 때** 부른다(배치 아님). 지금 돌리면 상권 카드가 안 붙는다.
  서비스는 안 죽고 8/21 배포로 **생략 사유는 화면에 표시**된다.
- ⚠️ **언제부터인지 알 수 없다(실측 2026-08-30).** 컨테이너 기동(8/30 01:57 KST — 8/29 브랜드스토어
  배포)부터 지금까지 `[sbiz365]` 로그가 **한 줄도 없다**. 로그 0줄 = 성공도 실패도 없음 =
  **그 사이 아무도 플레이스 분석·제안서를 안 돌렸다**. 내가 본 503 은 전부 **진단이 직접 부른 것**이다.
  ⇒ 오늘 직원 피해는 **0건**. 다만 API 가 죽어 있으니 **다음에 돌리는 사람부터** 상권이 안 붙는다.
  ⇒ 시작 시점은 컨테이너 로그가 그만큼밖에 안 남아 확인 불가.
- **✅ 경보 장치 — 대표 「나로해」 확정 · 배포 완료 (PR #170 · Deploy #412)**(안은 셋이었다):
  **가** 기록만(호출마다 성공/실패·시각 남김 — 「언제부터」에 즉답) ·
  **나** 가 + **매일 1회 자가 점검**(아무도 안 써도 죽은 걸 안다) ·
  **다** 나 + 관리자 화면 배지(**시안 확정 필요**).
  ⇒ 추천 **나** — 화면 무변경이고, 직원은 이미 그 자리에서 사유를 본다(8/21 배포). **대표 확정 = 나.**
  **무엇이 돌고 있나** — `backend/sbiz_health.py` 신설(표준 라이브러리만). 호출마다 성공/실패·시각을
  `sbiz_health` 표에 남기고, **매일 05:00** 표본 3곳으로 자가 점검한다(아무도 안 써도 죽은 걸 안다).
  ⚠️ **시도조차 안 한 경우는 기록하지 않는다**(업종 미선택·지역 미확정·키 없음) —
     그것까지 실패로 세면 경보가 곧 쓸모없어진다.
  호출처(`main.py` 2곳)는 한 글자도 안 고쳤다 — 같은 이름의 wrapper 를 씌워 배선을 그대로 뒀다.
  ✅ **8/31 확인 완료** — 05:00 첫 실행 `🩺 소상공인365 자가 점검 — 정상 (표본 1개 중 확인)` ·
  `sbiz_health` 에 `출처=probe` 1건 생성 · `region-unresolved` 0건. **스스로 도는 것까지 실증됨.**
- ⚠️ **이것 때문에 3축(시간대·요일 유동인구 / SNS / 테마상권) 판정을 미룬다.**
  20개 경로가 전부 503 이라 「경로 미공개」로 읽었다가 취소했다 — 정상 경로도 같이 죽어 있어
  503 만으로는 「없다」와 「지금 안 된다」를 못 가른다. **API 회복 후 재측정.**
  ⚠️ **교훈: 「없다」를 증명하려면 「있는 것」이 지금 되는지를 같은 시각에 재야 한다.**
  ✅ **API 가 복구됐으니 이제 잴 수 있다.** 게다가 `sbiz_health` 가 그 시각 API 상태를 남기므로
     다음부터는 같은 오판을 반복하지 않는다(「503 이라 없다」인지 「그날 API 가 죽었다」인지 표가 답한다).

#### 📌 다음에 할 것
- **✅ 축A 즉시 기록 경로에 자격 판정이 없다 — 배포 완료 (PR #166 · Deploy #410, 2026-08-30)**
  ✅ **8/31 확인 완료** — 오늘 기록 113개 전부 정상 · **누수 0 · 미연결 0**(어제 15·10).
  8/30 에 시각으로 갈라 「15건은 오전 잔상」이라 예측한 것이 하루 뒤 그대로 맞았다.
  아래는 원인 규명 당시 기록(그대로 둔다).
  실측(2026-08-30): 오늘 기록된 추적 상품 196개 중 **자격 없는 업체에만 이어진 것 15개** ·
  **어느 업체에도 안 이어졌는데 기록된 것 9개**(연결 없는 41개는 전부 비활성인데도).
  ⚠️ **08:00 배치가 아니다** — 배치는 `eligible_tracked_product_ids()` 를 제대로 지난다.
  새는 곳은 `rank_record._tracked_targets()` 로, 조건이 **키워드 일치 하나뿐**이다.
  같은 파일 `_client_targets()`(축B)는 8/29 PR #157 로 고쳤는데 **축A 만 빠졌다.**
  ⚠️ 이 경로가 `check_type="scheduled"` 로 적어 **배치가 쓴 것처럼 보였다** — check_type 은 경로가 아니다.
  **고치는 법**: `_tracked_targets` 에 같은 판정 함수를 건다. DDL 0 · 신규 경로 0 · 화면 무변경 ·
  조회 실패 시 「전부 기록」 폴백 유지. ⚠️ **미연결 상품을 무조건 빼면 안 된다**(방금 등록분이 죽는다) —
  비활성 표시된 것만 뺀다.
- **소상공인365 미개방 3축 프로브** — 위 503 이 **풀렸다(12:24 복구)**. 재측정 가능. 열리는 것만 목록화해 시안으로.
  ⚠️ **「매출 축 하나만 쓴다」는 틀렸다(코드 실측)** — 이미 5축이 나간다. 남은 건 셋뿐.
- **✅ 진단 워크플로 스텝 멈춤 — 원인 확정·고침·실측 확인 (2026-08-30)**
  ⚠️ **내 첫 가설(끝의 `sudo pvs/vgs/lvs/dmidecode` 가 tty 없이 암호를 물어 멈춘다)은 틀렸다.**
  명령별로 timeout 15초를 씌워 재 보니 **전부 0~1초**였다(docker ps·inspect·logs · free · df ·
  lsblk · sudo 4종 · 컨테이너 안 DB 읽기까지).
  **진짜 원인** = 그 스텝 마지막의 「DB 테이블별 행 수」 블록이 **3.1GB DB 를 통째로 스캔**한다
  (전 테이블 `COUNT(*)` + `client_analyses`·`collected_serp` 의 JSON 컬럼 `SUM(LENGTH(...))`).
  DB 가 커지며 스텝 예산 240s 를 넘기기 시작했다 — 11:47 실행은 통과, 11:50 실행은 242초에 timeout.
  ⚠️ 그 스텝이 안 끝나면 **뒤 스텝이 전부 실행되지 않는다**(그날 진단이 세 번 막혔다).
  **고침**: 그 블록을 기본 끔으로(`dbsize=on` 일 때만). 실측 확인 — 문제 스텝 **20초·성공**,
  뒤 스텝 전부 정상, 런 전체 51초 success.
  ⚠️ 교훈 — **멈춤의 범인을 짐작하지 말고 명령마다 timeout 을 씌워 재라.** 짐작(sudo)이 틀렸다.
  📌 덤으로 본 것: DB **3.1GB** · 디스크 36G 중 21G(63%) · 백업 회당 585~589MB(gzip, 2개 보관).
     지금 조치할 일은 아니지만 커지는 속도가 붙는다.

#### 🌿 머지 대기 중인 진단 브랜치
- **`diag/stages`** — `stages`(계약 단계 반영) · `axis_a`(축A 누수 정밀 측정). 읽기 전용 · 업체명 미출력.
  ⚠️ 다른 세션이 `diag/verify-408` 에서 같은 `debug-rank.yml` 을 만지는 중이라 **일부러 미머지**.
  그쪽 머지 후 both-keep 으로 올린다.
  ⚠️ 만들며 실수 2건 — `docker exec` 에 **`-i` 누락**(출력 0줄인데 success 로 끝난다) ·
  서브쿼리 **여는 괄호를 파이썬 괄호로 착각**해 `IN SELECT ...` syntax error.

#### ✅ 확인해 보니 이미 끝나 있던 것 (목록에서 내림 — 2026-08-29)
> ⚠️ **이 세션에서만 세 건이 이미 끝나 있었다.** 배포해 놓고 목록을 안 지운 탓이다.
> 목록의 추정은 **코드·실측으로 먼저 확인할 것** — 「고치면 주인 없는 상품 40개가 준다」고 적었던
> 메모도 재 보니 브랜드 주소는 0개였다.
- **계약 단계 이름 받아 두기** → **배포됨**(8/28 PR #152 · Deploy #404). `clients.contract_stage` 저장 중.
- **2대 분할(큐도 같은 규칙으로)** → **배포됨**(8/27 PR #145 · Deploy #401). `/keywords`·`/requests` 양쪽 적용.
- **등록 화면에 업체 선택 칸(필수)** → **배포됨**(8/28 Deploy #403). 서버도 400 으로 최종 차단.
- **[서랍 대기] 수집 커버리지 — 깊이·폭·IP 3택** → 대표 확인 「다른 섹션에서 완료」.
  8/28 개편으로 하루 346 → 759(2.2배)이고 **2대 확장 불필요 확정**.
  ⚠️ 원인은 IP 도 깊이도 폭도 아니라 **확장 워커가 30초 무활동으로 죽던 것**이었다 —
  제가 좁혀 둔 3택 자체가 틀린 축이었다.

#### ✅ 「계속 열려 있는 것」도 네 줄이 이미 끝나 있었다 (목록에서 내림 — 2026-08-30)

> ⚠️ **바로 위 칸(대표 확인 대기)을 실측으로 고친 그 날, 이 칸은 그대로 두었다.**
> 대표 질문(「그럼 요청한거 다 된거지?」)에 「상태 미확인」이라고 답한 뒤 WORKLOG 을 실제로 뒤져 보니
> **네 줄 다 결론이 나 있었다.** 「※ 상태 미확인」이라고 적어 두는 것은 답이 아니다 —
> **그 자리에서 WORKLOG 을 찾아볼 것**(제목으로 검색하면 1분이면 나온다).

- **🔴 공식 경로 사실 확인** → **확인 완료**(WORKLOG 2026-08-11). 네이버 공식 쇼핑 검색 API 는
  **2026-07-31 종료**(404 SE05)이고, 오가닉 순위를 주는 다른 공식 API 는 없다
  (데이터랩=트렌드 지수 · 검색광고 API=광고 지표). **결론이 난 사항이지 열린 작업이 아니다 — 재조사 금지.**
- **⚠️ ①에 통지·요청 — 맞춤제안서 플레이스 프록시** → **통지 불요로 종결.**
  2026-08-12 에 **①이 우리에게 역으로 통지**해 왔다(「광고주 공유 대시보드가
  `POST /api/place/proposal-enrich` 를 하루 1회 추가 소비합니다」) = **①이 이미 알고 쓰고 있다.**
- **⚠️ 맞춤제안서(플레이스) 실데이터 화면 미표시 사고 — 원인 3건** → **배포됨**(2026-08-05 대표 「배포하자」).
- **맞춤제안서 후속 — 품목 11종 리뉴얼 + 업종 「종합」 집계** → **배포됨**(위와 같은 배포).

#### 📋 계속 열려 있는 것 (1건)
- 소상공인365 상권 데이터 **추가 고도화** — 기본 연동은 이미 배포됨(`sbiz365.py` 1,077줄 동작 중), 시안 v2 대기

#### ✅ 확인해 보니 이미 끝나 있던 것 (목록에서 내림 — 2026-08-27)
- 백업 폴더 무한 증식 수정 → **배포됨**(gzip 압축 + `BACKUP_KEEP=2`)
- `client_analyses` 보관정책 → **매일 01:00 잡으로 돌고 있음**(`_run_client_analyses_retention`)
- 플레이스 순위 무인 추적 → **배포됨**(01:40 정리 잡까지)
- 맞춤제안서(플레이스) 실데이터 연동 → **배포됨**(`POST /api/place/proposal-enrich`)
- 수집기 차단 대응 → 대표 확정 「확장 프로그램으로 해결하고 있잖아」 — 별건으로 두지 않는다
- IP 재차단 + 오판 사슬 3건(v1.6.0) → 확장이 이미 **1.7.0** 이라 포함됨
- 보고서 저장 시각 → 대표 확정 「코드까지 만질 필요 없다, 한국 시간 표기만」

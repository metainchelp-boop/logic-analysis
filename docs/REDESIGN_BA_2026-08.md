# 전면 디자인 대개편 B+A (2026-08-05) — 연속성 메모리

## 결정 경위
- 대표: 「디자인이 구식·한눈에 안 들어옴 → 시안 3개 뽑아달라」 → 3종 비교
  (A 클린 라이트 콘솔 / B 사이드바 워크스페이스 / C 다크 프로) 전달 → **대표 선택 = B+A 조합**
  (사이드바 구조 × 클린 라이트 톤) → 확정판 4화면 클릭 시안 → 「이대로 진행」.
- 시안 파일: scratchpad `로직분석-대개편-확정판-BA-시안.html` (아티팩트 발행은 서비스 403으로 보류
  — 복구 시 기존 키워드순위 링크(09fd87cb)와 별도 신규 발행 예정).

## 배포 이력 (2026-08-05 새벽, 전부 무중단·회귀 게이트 통과)
1. **2차 확산 + 통합 뷰** (main 58ec78a) — 대시보드 카드(대표 키워드 순위·Δ·스파크)+KPI,
   로직 분석=스토어·플레이스 통합(PlaceClientPanel — /api/place/* 읽기 전용 소비),
   키워드 보드 정렬·7/30일·미노출 연속일, BE rank-overview 플레이스 제외+rep_series·
   rank-board days·unexposed_days.
2. **B+A 셸 교체** (main 2942d6f) — `AppShellBar.jsx` 신규: TopBar 와 동일 props 계약으로
   App.jsx 11개 블록 1:1 치환. 좌측 사이드바(쇼핑/플레이스/통합/관리 그룹·상승 뱃지·
   접기 localStorage·1080px 자동)+상단 바(크럼·전역 검색 Ctrl+K→대시보드 핸드오프
   sessionStorage `logic_global_q`+`logic-global-search` 이벤트·사용자)+1회 안내 칩
   (`logic_nav_intro_v7`). 본문은 body padding 으로 밀어냄(페이지 내부 무수정).
   **구 TopBar.jsx 는 미사용 보존(비상 롤백: App.jsx 치환만 되돌리면 됨).**
3. **블루 토큰 전면 통일 + v7.0.0** (main 첫커밋 be333a5241b2 번들) — 43개 컴포넌트+styles/place.css:
   #6c5ce7/#4f46e5/#6366f1→#3b82f6 · #a29bfe→#93c5fd · #5f3dc4→#2563eb · 활성 필 네이비→블루.
   **보존(시맨틱)**: AI 보라 #7c3aed · 관리자 배지 #6d28d9 · rankImage.js 보고서 이미지 헤더
   (광고주 전달물 불변) · report-capture.js(보고서 캡처 토큰 기존 유지).

## 다음 세션 주의
- 대표 방침: **「기존(구식) 디자인 유지 가급적 금지」** — 신규 화면·수정 화면은 확정 시안의
  블루 토큰(#3b82f6 계열)·화이트 카드·KPI→필터→표 문법을 따를 것. 구 보라 재도입 금지.
- 잔여 마감(소소): 화면별 간격·타이포 미세 통일, 플레이스 페이지 내부 구조 리터치(타 세션
  활동 안정 후), 아티팩트 발행 재시도.
- 사이드바 메뉴 추가 시 `AppShellBar._AS_GROUPS`·`_AS_CRUMB` 두 곳.
